# 2026-08-16 调研：stablyai/orca 二开可行性 + fork 同步策略

## 需求
用户 fork 了 stablyai/orca（GitHub: penghuizhang/orca），计划基于此二开定制功能，同时希望持续同步上游更新。

## 调研结论（上游状态：2026-08-16 当天数据）
- **项目定位**：Orca = ADE（Agent Development Environment），Electron 桌面应用，可在独立 git worktree 中并行运行 Codex/ClaudeCode/OpenCode 等 agent，含移动端配套 app（mobile/）与原生模块（native/）。
- **技术栈**：TypeScript + Electron + React(shadcn/ui) + pnpm workspace；约 15k 文件。
- **协议**：MIT —— 二开无许可障碍。
- **活跃度极高**：46k+ stars；几乎每日发版（v1.4.182/183/184-rc.0 均为 8 月中旬几天内）；每天 10+ commit。
- **上游代码质量门禁严格**：oxlint + max-lines-ratchet + reliability-gates + localization 校验（AGENTS.md 明令禁止 max-lines disable）。
- **扩展机制（二开关键）**：内置 `skills/` 目录（computer-use、orca-cli、orchestration 等），类似 Claude Code skills 机制；`orca.yaml` + `config/` 提供配置面；`config/patches/` 为 pnpm patch 机制。→ 部分"定制功能"可在 skill/配置层实现，不动核心 src/，与上游冲突面最小。

## 已执行动作
1. 本地 clone fork 到 `/Users/zhangpenghui/code/aistudy/2026/fork/orca`（之前为空目录）。
2. 配置 upstream remote：`git remote add upstream https://github.com/stablyai/orca.git`。
3. 确认 fork main 与上游完全同步（0 ahead / 0 behind），干净起点。

## 关键决策：二开文档与记忆存放
- 上游自带 `docs/` 与根 `AGENTS.md`（属于上游代码库，同步时会被覆盖/冲突）。
- **二开的设计/影响文档与归档一律放 `.workbuddy/` 下**（如 `.workbuddy/docs/`），绝不写进上游 `docs/`，避免与上游文件混同造成 sync 冲突。
- 工程记忆目录 `.workbuddy/memory/`（本次已建立）。

## 推荐工作流（待用户确认后实施）
- 分支模型：`main` 保持纯净只跟踪上游；定制代码走独立分支（`custom/` 主分支 + feature 分支）。
- 同步节奏：上游日更，建议按需/每周同步，用 merge 而非 rebase（公开 fork 不 rewrite 历史）。
- 冲突面控制：定制优先"新增文件"（skills/ 新增、独立模块），避免改上游热文件。
- 通用功能优先向上游提 PR，保持 fork 纯净；私有定制留本地。

## 打包与本地使用方式调研（同日补充）
- **开发期**：`pnpm dev`（electron-vite dev，从源码跑完整应用，热重载），无需打包安装。
- **正式使用**：`pnpm build:mac` → `config/scripts/build-mac-local.mjs`，本地 electron-builder 打包，`ORCA_LOCAL_BUILD_VERSION=local.<ts>.<commit>`，**不需要 Apple 开发者证书**（非 ORCA_MAC_RELEASE 路径）。产物在 dist/（dmg/app）。
- `build:mac:release` 才需要开发者证书签名+公证（verify-macos-release-env.mjs），二开自用不需要。
- **appId 为 `com.stablyai.orca`**：本地二开包会与官方版同 id 覆盖安装；若需共存须改 bundle id/productName（留待定制时处理）。
- **macOS Gatekeeper**：本地 adhoc 未公证包首次打开需右键-打开或 `xattr -cr`。
- **环境要求**：engines node=24、packageManager pnpm@10.24.0、git-lfs。
- **本机环境现状**：node v22.22.3（系统）+ nvm 最高 v22.15.1 → **缺 Node 24，需先 `nvm install 24`**；pnpm 10.24.0 ✓、git-lfs ✓。
- 首次 `pnpm install` 很重：Electron 下载 + 原生模块编译（node-pty 等），预计 10-30 分钟。

## 待办
- [x] 与用户确认二开目标功能清单 → 划定 skill 层 vs 核心层改动（**等用户提供功能清单**）
- [x] 建立 `custom/` 分支（2026-08-16 完成：基于 main 1f483c3e4 创建，本地未推送；后续定制代码与 .workbuddy/ 记忆均提交于此）
- [ ] 同步脚本/文档（每周 fetch upstream → merge main → merge custom/）
- [x] 装 Node 24 → `pnpm install` → `pnpm dev` 验证环境（2026-08-16 完成，见上）
- [ ] 二开完成后 `pnpm build:mac` 打安装包（届时处理 appId 共存问题）

## 环境搭建过程记录（✅ 已完成 2026-08-16）
- Xcode 完整版已装（/Applications/Xcode.app）✓，原生模块编译无前置障碍。
- `nvm install 24` 成功 → v24.19.0。
- 【坑】本机 nvm：`nvm use 24` / `nvm use v24.19.0` 均失败（exit 3，静默）；**交互 shell 必须 `nvm use 24.19`**；非交互/后台 shell 连 `nvm use 24.19` 也不稳定 → **最稳方式：`export PATH="$HOME/.nvm/versions/node/v24.19.0/bin:$PATH"`**。疑似 nvm alias 解析问题（default -> v14.20.0 指向不存在版本）。
- 全局 npmrc 已配 npmmirror + electron_mirror，`pnpm install` 仅 2 分钟。
- `pnpm install` 成功（exit 0）：Electron 43.1.0、node-pty 源码重建成功、husky 就绪。pnpm 10 忽略 6 个构建脚本（@swc/core、agent-browser、electron-winstaller、msw、ssh2、windows-native-registry），上游未 approve，不影响 macOS。
- `pnpm dev` 验证通过：preload 29ms、renderer :5173、Electron 窗口打开（PID 存活）。日志无害项：MaxListenersExceededWarning、legacy worker recovery failed (terminal_liveness_unavailable)。
- dev 用户数据目录 `~/Library/Application Support/orca-dev`（与正式版隔离）。
- 验证产物：`.workbuddy/docs/2026-08-16-本地开发环境搭建.md`（设计+验收+实施记录）。

## 项目体检报告（同日补充，纯调研未动代码）

- 产出：`.workbuddy/docs/2026-08-16-项目体检报告.md`（8 问全答，含行号证据）。报告放 .workbuddy/docs/ 而非上游 docs/（遵守既定决策，防 sync 冲突）。
- 关键数据（2026-08-16 实测）：46,181 stars / 3,234 forks / 3,935 open issues；建仓 2026-03-17；本地 8,747 commits，近 30 天 1,997 个（≈66/天）；latest release v1.4.183（08-15）。
- 技术栈实测：322.5 万行 / 15,331 文件；.ts 77.2% + .tsx 14.3% ≈ 91.5%；src/renderer 130.7 万行 ≈ src/main 107.9 万行（前后端五五开）；**52% 行数为 *.test.ts**（153.7 万行，orca-runtime.test.ts 单文件 5 万行）。
- 依赖：根 23 运行时 + 100 开发 + 6 可选；mobile 46+15；engines node=24 硬性、pnpm@10.24.0 精确锁、5 个 pnpm patch（node-pty/xterm）。
- 原生模块语言：Swift（computer-use-macos/notification-status-macos）、Python（computer-use-linux）、PowerShell（computer-use-windows）、C#（windows-cli-launcher）。
- 二开难度初判 **4/5**（体量大 + 多栈 + 门禁严 + 上游日更同步压力 + 测试重）；缓解面 = MIT + skills/ 扩展机制 + CLI。
- 约束核对：本次无代码改动、无 SQL、无安装运行，约束清单不适用；repomix-output.xml 不存在，已直接读仓库替代。
- 【更正】体检报告最终位置：`docs/2026-08-16-项目体检报告.md`（仓库根 docs/，非 .workbuddy/docs/）。原因：本机 doc-workflow stop hook（~/.zcode/hooks/doc-workflow-hook.sh）只校验 `root/docs/*.md`，不认 .workbuddy/docs/。已从 .workbuddy/docs/ 移入 docs/。sync 风险评估：文件名含日期+中文，上游不可能同名，merge 无冲突面；唯一副作用是 fork main 多一个非上游文件（归属清晰，与「混同」风险不冲突）。后续若定制分支策略落地，可在 custom/ 分支上管理该文件。

## 第一个二开任务：Gitee 集成（评估阶段，未动代码）

- 需求：用户代码主要托管 Gitee（GitHub 网络受限），要集成 Gitee。
- 关键调研发现：**Orca 已是多 provider 架构**——src/main/source-control/forge-provider.ts 定义统一 ForgeProvider 接口，注册表已有 5 个 provider（GitLab/GitHub/Bitbucket/Azure DevOps/Gitea，:296-299）；provider 判定 = 注册表顺序探测 resolveRepository（:309-317），**加 provider = 新增实现 + 注册一行**，判定链不用动。
- 认证模式差异：GitHub 走 `gh` CLI、GitLab 走 `glab` CLI、Gitea/Bitbucket/Azure 走 HTTP + 自有 token 存储（bitbucket/credential-store.ts）。**Gitee 无官方 CLI → 必须 HTTP + PAT 模式**，仿 Bitbucket。
- Gitee API v5 实测（公开只读无需 token）：`GET /api/v5/repos/{owner}/{repo}` ✓、`/pulls?state=open` ✓，结构与 GitHub API v3 高度相似（字段有差异，实施时对照 swagger 逐接口验证）。
- git 层现状：clone/push/pull 对 gitee.com **已可用**（git 不挑远端），缺的是 provider 识别与 PR/Issue 类应用层集成。
- 分级方案（详见设计文档）：L0 远端识别（≈1 天，新增文件+注册表追加）；L1 认证(PAT)+PR/Issue 只读浏览（≈1-2 周）；L2 创建 PR/评审/CI（大工程，按需排期）。
- 待用户拍板：① 范围 L0 还是 L0+L1；② PAT 先做还是 OAuth2；③ 是否支持 Gitee 企业路径。
- 设计文档：`docs/2026-08-16-Gitee集成评估与方案.md`（含 6 条约束清单，实施时自查）。
- 当前分支 custom/ 尚未提交任何内容（docs/ 被 gitignore，.workbuddy/ 未跟踪）。

## Gitee 集成实施：L0 完成（2026-08-16）

- 用户拍板：L0+L1 全做，L2 排期；认证先 PAT（OAuth2 二期）；范围=名下全部公开+私有仓库。
- 分支模型修正：【坑】git 不允许 `custom` 分支与 `custom/xxx` 子分支并存（cannot lock ref）→ feature 分支命名改用平级 `gitee-integration`（自 custom 拉出）。后续 feature 分支统一用 `xxx` 平级命名。
- L0 已提交：commit 8a8060133（分支 gitee-integration，14 文件 +232/-2，未推送）。
  - 新文件：src/main/gitee/repository-ref.ts（GiteeRepoRef + parseGiteeRepoRef，只认 gitee.com/www.gitee.com 精确 host；复用 createRemoteRefProbeCache + detectGitRemoteIdentity 的 SSH 路由）、forge-provider.ts（giteeForgeProvider，PR 查询占位 null）、repository-ref.test.ts（9 用例）。
  - 修改：shared/hosted-review.ts（HostedReviewProvider 加 'gitee'）、shared/pull-request-generation.ts（PROVIDER_LABELS）、main/source-control/hosted-review.ts（case 'gitee'）、forge-provider.ts（import+注册表尾追加）、forge-provider.test.ts（预期表加 ['gitee', false]）、renderer 6 处穷举 switch 空实现（linkedGiteePR 元数据 L1 再接）。
- 验证：node/web/cli 三个 typecheck 全过；相关单测 42/42 过；lint-staged（oxlint+oxfmt）在 commit 时自动跑过。
- 约束自查全过（见设计文档「L0 已落地内容」），剩余已知限制：SSH alias host 不识别、企业嵌套路径不识别、PR 查询 null（均 L1 处理）。
- L1 下一步（待实施）：PAT 认证存储（仿 src/main/bitbucket/credential-store.ts + integration-credential-file）→ src/main/gitee/client.ts（API v5，pulls/issues/repos，实测字段）→ IPC/RPC 接线 → 渲染层 UI（GitLabItemDialog 对照）→ linkedGiteePR 元数据接线 → 测试验收。
- 设计文档：docs/2026-08-16-Gitee集成评估与方案.md（含最终决策与排期表）。

## Gitee 集成 L1 完成（2026-08-16，分支 gitee-integration，5 个提交未推送）

- L0 8a8060133 → L1.1 051e75905（PAT 认证）→ L1.2 4e8171c86（API 客户端）→ L1.3+1.4 16f4c816e（IPC+UI）→ 5a37ee56b（i18n）。
- Gitee API v5 实测关键差异（已写进 shared/gitee-api.ts 注释）：issue number 是字母数字字符串（IJZ86Z 而非整数）；repo html_url 带 .git 后缀；merged PR = state=closed + merged_at 非 null；draft = state=open + draft=true；/user 无 token 或坏 token 均 401（"Access token does not exist"）。
- 认证链：env（ORCA_GITEE_ACCESS_TOKEN）优先 → ~/.orca/gitee-credential.{json,enc}（复用 integration-credential-file safeStorage 加密，仿 bitbucket credential-store）；连接前先 GET /api/v5/user 验证（401/403=rejected，网络/5xx=unreachable）。
- 客户端：requestGiteeJson 统一错误分类；/user/repos?type=all 列名下全部公开+私有仓库；pulls/issues 列表；PR 按 head.ref 分页扫描（最多 5 页×100）。
- forge 接线：giteeForgeProvider 的 getReviewForBranch/ByNumber 已接真实查询；HostedReviewInfo.status 恒 'neutral'（L1 无 CI 状态，避免伪造成功/失败）；mergeable 映射 MERGEABLE/CONFLICTING/UNKNOWN。
- UI：Settings → Review providers 新增 Gitee 卡片（preflight 状态 + Connect 对话框 + Browse repositories 对话框：仓库列表 + PR/Issue tab + 打开网页/复制 clone URL）。preflight payload 加 gitee 字段（preflight.ts / preflight-api.ts / integrations-pane-status.ts 三处同步）。
- i18n：sync:localization-catalog 自动补 en.json 48 个 key（auto.components.settings.gitee.*）；verify:localization-extraction 通过。
- 验证全绿：node/web/cli typecheck、gitee 单测 26 + 相关 22、electron-vite 全量 build、oxlint/oxfmt、localization。
- 【坑】renderer oxlint 规则 require-styled-vertical-scrollbar：overflow-y-auto 滚动容器必须带 scrollbar-sleek/scrollbar-editor/worktree-sidebar-scrollbar 类。
- 【坑】DialogContent 无 contentClassName prop，用 className（cn 合并）。
- 待用户验收：用本人 Gitee 私人令牌在 Settings → Gitee 卡片连接，验证私有仓库列表 + PR/Issue 浏览。L2 排期待验收反馈。
- 分支模型再确认：feature 分支平级命名（gitee-integration），不要用 custom/xxx（git 限制）。

## 研发云平台集成调研（任务暂停，未完成）

- 需求：把公司研发云平台（www.srdcloud.cn，中国电信，devops@chinatelecom.cn）集成进 Orca，免登录查看代码库/提交/Bug 等；另评估本地 codefree-O CLI 的 MCP 技能可参考性。
- 已探明事实（2026-08-16 实测）：
  - 平台主站是 Vue SPA（"研发云，一站式云上开发"），帮助中心内容走登录墙，未登录只能看到登录页（天翼账号/云认证 OAuth，open.e.189.cn 统一登录）。
  - 平台网关模式：/api/{service}backend/{module}/v{n}/...（例：/api/portalbackend/portal/webconfig/env-rsrc、/api/usercenterbackend/userlogin/v1/get-captcha、codefree 配置里的 /api/acbackend/codechat/v1）。帮助中心内容接口名被 JS 混淆（chunk 懒加载），未抓到。
  - 子域：docs.srdcloud.cn = 石墨文档（需登录）；gz01-srdart.srdcloud.cn = Artifactory（maven/docker 制品库）；ux.srdcloud.cn = mastergo 设计稿。git 域名尚未确认（大概率 gz01-srdgit 之类，待查）。
  - 本机 codefree 配置 ~/.codefree-config.json：apiEndpoint=www.srdcloud.cn、chatApiPath=/api/acbackend/codechat/v1、subService=codefree-plugin-composer-chat（含 apiKey，勿外传）。codefree-o 是 @opencode-ai/plugin 插件框架，.config/skills 与 skills-market 缓存均为空，未见 MCP 技能清单（用户以为有很多技能，实际待确认）。
  - 用户企业内网 maven/docker 仓库均指向 srdcloud（zhdzjyy 等工程），说明平台承载真实研发流程。
- 结论初判：平台无公开 OpenAPI 文档、全站登录墙 → 集成必须走「内部 API + PAT/SSO 凭证」路线，需先向平台侧确认开放 API（很可能有企业版开放平台）。成本评估未完成。
- 状态：暂停（用户指示终止探索）。续接点：① 确认平台开放 API/文档；② codefree 插件市场/IDE 扩展源码里找平台 REST 接口清单；③ 对照 forge-provider.ts 架构出分级方案。

## Gitee 验收事故与修复（2026-08-16 下午）

- 现象：用户验收时 Browse repositories 报「No handler registered for 'gitee:listRepos'」——preload 有方法、main 没 handler。
- 根因：L1.3 对 src/main/ipc/gitee.ts 的扩展 Edit 报「File has been modified since read」后**没有重读重试**，listRepos 代码从未落盘；但 preload/类型/UI 都提交了（16f4c816e 只含旧版 gitee.ts），单测只测了 client 层没测 IPC 层，typecheck 不查 IPC 通道名一致性 → 全部验证绿但运行时炸。
- 修复：commit 853fc0609 补上三个 list handlers；重启 dev 后 grep out/main/index.js 确认含 gitee:listRepos。
- 【教训-重要】① Edit 失败必须 Read 后重试，绝不跳过；② 多进程接线（preload↔main IPC）改完必须 grep 构建产物验证两端都有，不能只信 typecheck；③ dev 模式 Electron 主进程不热重载，main 改动要重启进程。
- 用户 token（46f4613f...）API 实测有效（login zhangpenghui）；已提醒用户验收后撤销该 token（曾出现在聊天记录）。

## Gitee L2-A：首页 worktree 卡片 PR 显示（2026-08-16 晚，commit 76a34cfb3）

- 需求：首页任务/worktree 卡片像 GitHub 一样显示 Gitee PR 与议题；复用现有面板体系。
- 实现：linkedGiteePR 元数据全链接线（shared/worktree 类型 4 处 + hosted-review 2 处 + persistence 默认值 + orca-runtime 8 处透传 + runtime-folder-workspace + ssh-adoption + selected-review-branch 判定 + hosted-review.ts 的 reviewLinkForProvider + renderer 显示链 6 文件 + hosted-review-cache-identity 的 hint key）。
- 显示闭环：Gitee 仓库分支上有 PR（head.ref 匹配）→ forge getReviewForBranch 查到 → worktree 卡片显示 #编号·状态（open/merged/draft/closed）→ 点击跳 Gitee 页面；linkedGiteePR 为显式关联（粘贴 URL 解析留 L2-B）。
- 【坑】max-lines 门禁：parent-pr-checks-rows.ts 加 1 行参数即超限（301/300），不能加 disable → 压缩 summarizeParentPrChecksRows 的 unknown 数组（6 项单行化拆 2 组）净减 3 行。经验：改这种文件前先看 wc -l 余量。
- 验证：三个 typecheck 全过、相关单测 44/44、oxlint/oxfmt 过；dev 已重启。
- 待办：L2-B = Gitee URL 粘贴解析进 linkedGiteePR、TaskPage 深度集成（PR 过滤器/issue 选择器/从 issue 开 worktree）、创建 PR、评审注释、CI 状态、OAuth2。

## 第二个二开任务：日历功能（设计阶段，未动代码，待评审）

- 需求：侧边栏左上角（Automations 旁）新增「日历」入口；月视图排期；条目可填会议/功能点/里程碑/描述；本地存储。
- 调研结论：Automations 就是同构范式（shared 类型 + persistence orca-data.json + ipc + preload + SidebarNav + AppWorkspaceShell 挂载 + ui.ts 导航）；TopLevelView 加 'calendar' 需同步 `shared/top-level-view.ts` 的 Record 穷举（漏加 typecheck 必报）+ renderer 14 处枚举点逐一核对。
- 关键决策：不引第三方日历库（避免动 pnpm 锁文件）；月网格自研纯 React；分类色板 4 色（meeting/feature/milestone/other）；日期 YYYY-MM-DD 本地无时区；数据存 orca-data.json（calendarEntries 字段 + normalize）。
- 分支：calendar-feature 自 custom 拉出（不叠 gitee-integration）。
- 设计文档：docs/2026-08-16-日历功能设计方案.md（10 条约束清单，待用户评审后实施）。

## Gitee L2-B：首页 Tasks 面板接入 Gitee 数据源（commit 78c91d516 + f2504e57c）

- 需求：首页任务/议题/PR 面板像 GitHub 一样显示 Gitee 内容（用户验收时发现只有 GitHub issue 列表）。
- 实现：TaskProvider 加 'gitee'（shared/task-providers.ts）+ 全部穷举 switch/Record 连锁（task-provider-identity、automation ×2、chrome-visibility、context-summary、TasksPane、readiness、workspace-name、测试 mock ×2）；TaskPage 加 taskSource 'gitee' 分支（slim 模式照抄 gitlab：view=pulls/issues + 账号级聚合列表 + 跳转）；main 端 listAccountPulls/listAccountIssues（/user/repos → 并发 8 逐仓库查 open pulls/issues，限 50 仓库 × 5 条）；getSourceOptions 加 Gitee 切换项。
- Gitee 无账号级 PR 端点（/issues 账号级实测返回空）→ PR 必须按仓库聚合。
- 【重大事故】另一会话（calendar-feature 任务）在我工作时切分支 + git reset/stash 了我的工作区：L2-B 未提交改动一度全丢，靠 stash@{0}（"gitee L2-B WIP"）恢复；preload/index.ts 还混入 calendar 会话的半成品（CalendarEntry 类型），已拆分提交（我的 hunk 进 gitee 提交，calendar 部分留在工作区）。**教训：多会话并行操作同一仓库时，改动要及时提交 + 提交前 git status 检查分支与文件归属。**
- 【坑】max-lines 临界文件（299±2 行）加 1 个 case 就超限：automation-target-availability / task-source-context-summary 需压缩 switch 单行化（`case 'x': return 'y'`）。
- 验证：web/cli typecheck 过（node 被 calendar 半成品污染，其错误与本次无关）；相关单测 77/77；oxlint/oxfmt 过；dev 已重启（bundle 含 listAccountPulls）。
- 验收路径：首页顶部任务来源切换 → Gitee → Pull requests / Issues tab → 全部仓库聚合列表。

## Gitee L2-B 补漏：侧边栏 Tasks 入口（commit ce2d89874）

- 用户验收发现看不到 Gitee 入口：根因是任务来源切换器实际渲染在 **SidebarTaskNavButton.tsx**（左侧栏 Tasks 按钮 hover 时显示 provider 图标组 github/gitlab/linear/jira），只列了 4 个——漏 gitee（TaskPage 的 sourceOptions 加了也没用，入口不在这）。
- 修复：SidebarTaskNavButton 加 gitee TaskProviderShortcut（FolderGit2 图标，openTaskPage({taskSource:'gitee'})），提交 ce2d89874。renderer 改动走 vite HMR，无需重启。
- 【坑】本机环境代理（ZCODE_HTTP_PROXY/HTTP_PROXY=127.0.0.1:54687）会让 curl localhost:5173 返回 502 假象——探测本地 vite 必须 `curl --noproxy '*'`。
- 【坑】5173 曾被用户另一项目（tubaobao webview-app）的 vite 占用（PID 438）；当前 Orca vite（PID 24066）绑 IPv6 localhost:5173，正常。
- 验收入口：左侧栏 hover「任务」→ 出现 5 个 provider 图标 → 点 Gitee（文件夹图标）→ PR/议题聚合列表。

## 同日补充：zcode 多模态（图片识别）配置诊断

**需求**：DeepSeek 主模型无视觉，read-image skill 子代理委托不稳定，切换模型仍报图片不能处理。

**根因（三层，均已实测验证）**：
1. 主模型商 `opencode-go`（opencode.ai/zen/go）只有 deepseek-v4-flash/pro，网关协议层拒绝 image_url（`unknown variant image_url`）。
2. read-image skill 声称委托 `opencode/mimo-v2.5-free` —— 用户已在桌面版 v2 配置该模型，但**网关实测返回 "Model mimo-v2.5-free is not supported"**（模型名无效），且其 modalities.input=["text"]（无视觉标注）。
3. 用户可切换的全部模型（deepseek×2、glm-5.2、gpt-5.6-luna、mimo）均无视觉；glm-5.2 网关收图但模型自述无多模态能力。

**实测唯一可用通道**：智谱 API key（v2/config.json `builtin:bigmodel-coding-plan` 的 key，a54f0e1a...）调 **`glm-4.1v-thinking-flash`（免费视觉模型）** 成功识别图片；付费视觉模型 glm-4.6v/4.5v/5v-turbo 余额不足（1113）。

**已做配置（可逆）**：
- `~/.zcode/cli/config.json`：新增 `bigmodel` 商（openai-compatible，baseURL=open.bigmodel.cn/api/paas/v4，apiKey=智谱 key，models=glm-4.1v-thinking-flash/glm-4.6v）；`subagents.builtInModelOverrides` 指向 bigmodel/glm-4.1v-thinking-flash。
- `~/.zcode/v2/agents-state.json`（新建）：同款 builtInModelOverrides（源码确认此为桌面版子代理模型覆盖的读取路径）。
- **注意**：CLI `--prompt` 单次模式实测覆盖未生效（子代理仍走 deepseek），需桌面版重启新会话验证。

**待验证**：用户重启 zcode 桌面版贴图测试；若仍失败 → 智谱充值开 glm-4.6v 或主模型换原生视觉模型（Claude/Gemini）。

**归档**：影响说明文档 `docs/2026-08-16-zcode多模态配置诊断与影响.md`（工程根 docs/，hook 校验通过）。

## 任务页 Gitee 看板:入口可见性修复(2026-08-16 下午,commit 161a378e3)

- 需求:截图红框「任务」页签,红字「添加gitee相关的功能,功能内容和github的相同」;口述「Get 议题」= Gitee 议题看板、「Get Help」= GitHub(语音转写误听)。
- 根因:**L2-B(78c91d516)已实现全部功能**(任务页来源图标条含 Gitee、PR/议题聚合看板、侧边栏入口),但用户持久化设置 `visibleTaskProviders=['github','gitlab','linear','jira']`(Gitee 加入前保存)→ `normalizeVisibleTaskProviders` 只过滤不追加 → 任务页头部图标条与侧边栏 hover 均隐藏 Gitee,用户进不了看板。
- 修复:照搬上游 jira 一次性迁移模式 → 新增 `visibleTaskProvidersDefaultedForGitee`:类型(global-settings-types.ts)+ 默认值 true(constants.ts)+ persistence.ts 加载迁移(flag 未置位且缺 gitee 则追加)+ loadNeedsSave + 全量保存盖章 + 部分更新盖章;测试更新 4 处 + 新增 2 处。
- 验证:worktree 内 83/83 单测过、typecheck:node 全绿、oxlint/oxfmt 过。
- 【重大事故-第二次,教训再升级】另一会话(calendar-feature)15:00:44-52 对共享工作目录执行 git reset + git checkout calendar-feature,我的 8 文件未提交改动**全部被 checkout 丢弃**(连 index.lock 撞车都没拦住)。恢复套路:临时 worktree(`git worktree add /tmp/orca-gitee-fix gitee-integration`)→ 按会话上下文重放全部改动(python 精确匹配+断言)→ symlink 主仓 node_modules → 测试/typecheck 全绿 → commit → worktree remove。**教训:共享工作目录随时可能被其他会话切分支,改动必须尽快提交;未提交内容=可丢弃内容。**
- 验收路径:切回 gitee-integration → 重启 dev(迁移自动给用户设置补 gitee)→ 任务页图标条点 Gitee → PR/议题看板。
- 设计文档:`docs/2026-08-16-任务页Gitee看板.md`。

## zcode 图片识别模型续测(2026-08-16 下午)

- 用户指示:后续图片识别使用 `mimo-v2.5-pro`。实测:opencode 网关存在该模型(文本回复 OK),但**图片输入被网关拒绝**("404 No endpoints found that support image input")——与 mimo-v2.5-free 同类,无视觉能力。
- 结论:图片识别仍走智谱 `glm-4.1v-thinking-flash`(直接 curl open.bigmodel.cn,免子代理/免余额,本日两次实测成功);mimo-v2.5-pro 勿配为视觉模型,已记入全局记忆。

## 任务页 Gitee 看板:形态对齐 GitHub(2026-08-16 下午,验收反馈迭代,worktree /tmp/orca-gitee-accept)

- 用户验收截图对比:GitHub 看板有「议题/PR/项目」页签 + 筛选条(开放/分配给我)+ 搜索框(is:issue is:open)+ 五列表(ID/标题/负责人/状态/已更新);Gitee 只有「Pull requests/Issues」+ 四列表 → 要求完全相同。
- 实施(未提交,待验收):① 数据层 shared/gitee-api.ts:RawGiteeIssue 加 assignee?、RawGiteePull 加 assignees?、GiteeAccountItem 加 assigneeLogin;② main/gitee/client.ts:两处映射加 assigneeLogin,聚合改为同时拉 open+closed(ACCOUNT_AGGREGATE_STATES)并按 updatedAt 降序;③ TaskPage gitee 分支:页签改「议题/PR」(复用 GitHub 同款 i18n key 137e2a8a01/dfc0c79bd8)、状态筛选 chips(开放/已合并(仅PR)/已关闭/全部,复用 606a85c774 等 key)、本地过滤搜索框(新增 gitee.searchPlaceholder/gitee.noMatching 2 key)、五列表(ID/标题+仓库副行/负责人/状态/已更新)。
- 【坑】聚合接口原先只拉 state=open,「已合并/已关闭」筛选会永远空 → 必须 open+closed 双请求,chips 才有意义。
- 验证:typecheck node/web 全绿(worktree 无日历污染)、gitee 单测 26/26、oxlint/oxfmt 过、sync:localization-catalog 自动补 2 key(+openTasks 历史遗留 key)。
- 设计文档:docs/2026-08-16-任务页Gitee看板.md(追加「形态对齐 GitHub」节)。

## 日历优化轮：点击建条目 + 国际化补全（2026-08-16 下午，calendar-feature，未提交）

- 需求（用户反馈 3 问题）：① 点月视图日期格不弹窗建条目；② 日历界面全英文不跟随系统中文；③ 侧边栏入口英文。
- 根因：**zh.json 缺整个 `auto.components.calendar.*` 子树 + `SidebarNav.calendar` 键**（en.json 早有 36 键——上会话只跑了 en 侧 sync 漏了 zh 侧手工补）；组件代码其实全走 translate()，缺键全落英文 fallback。另发现 en 键冲突 bug：`auto.components.calendar.title="Calendar"` 被页头标题与对话框「标题」标签复用 → 英文界面表单标签也错显 "Calendar"。
- 改动：① `calendar-month-grid.tsx` 新增 `onRequestCreate(dateKey)` prop，格子点击=选中日+打开新建弹窗（默认日期=所点格；chip 的 stopPropagation 编辑行为不变；侧栏迷你月历仍仅选中）；② en.json `title→"Title"`、新增 `pageTitle="Calendar"`、`deleteEntryDescription` 改 `{{title}}` 插值（原 JSX 拼接 'Delete'+title+'?'）；③ zh.json 补 36 键（分类=会议/功能点/里程碑/其他，删除确认=「确定删除「{{title}}」？」）+ `SidebarNav.calendar=日历`。
- 验证全绿：verify:localization-catalog（zh 11814→11851 键、en/zh 36 键一一对应、插值一致）/extraction/coverage 三过、三 typecheck 过、日历单测 27/27、oxlint 0 错误、oxfmt 全 10 个日历文件已格式化（含前一会话遗留未格式化文件）。
- 【教训-可复用】项目 i18n 惯例：**en.json 由 sync:localization-catalog 自动补，zh/es/ja/ko 必须手工补键**（缺键=该语言用户看英文 fallback）；改界面文案先查 zh.json 对应子树是否存在。
- 【提醒-重要】**calendar-feature 分支至今零提交**（git status 全 M/untracked），共享工作目录有被其他会话切分支清空的风险（前两次事故教训），需尽快提交。
- 设计文档：`docs/2026-08-16-日历优化-点击建条目与国际化.md`（含约束核对表）。

## 日历优化轮：dev 重启（2026-08-16 15:46）

- 用户要求重启服务：旧 dev 实例已停（5173 无监听），`pnpm dev` 后台重启成功——vite :5173 + Electron 窗口（out/electron-dev/.../Orca: calendar-feature.app，PID 74122）+ 主进程 bundle 含 calendar:list IPC。
- 本轮改动（格子点击建条目 + zh 国际化）已随新实例生效；用户可在中文界面验收（侧边栏「日历」、点格子弹窗、全中文文案）。
- 【备忘】日期语言包 zh.json 懒加载，旧实例需要刷新窗口才生效；本次全新启动无此问题。

## ZCode 会话标题自适应生成调研（2026-08-16 下午，工具链，未实施改动）

- 用户疑问：ZCode（我们这份 = zcode-app-cli + ZCode.app 桌面版）能否像 opencode 一样根据任务描述自适应生成会话标题；「A 秀」为转写误词（本会话自动标题即「创建 A 秀实现自适应标题生成」）。
- **结论：原生支持且已正常工作**。证据：① cli db session 表含 title/title_source(default|first_input|generated|custom)/title_message_id/time_title_updated；今日分布 generated 104 / first_input 86 / custom 25，标题质量好；② 本会话标题由首条消息自动生成（title_message_id 指向首条 user 消息）；③ 内核 vendor/zcode.cjs：SessionTitle 任务（trigger:"turn"）、prompt「Generate a concise title…用户语言 3-7 词、保留专有名词、JSON 输出」、超时 60s/maxTokens 1200/长度上限 100、配置开关 titleGenerationEnabled（默认开，显式 false 才禁用）；④ 桌面版 app.asar 同源（titleGenerationEnabled 9 处）。
- 【坑-认知】个别会话标题仍是首条消息原文属**正常跳过**：not_eligible（子代理/任务会话）、custom_title（手动改过）、first_query_edited（首条消息被编辑）、unchanged_title、stale/target_changed 竞态——不是「没这功能」；opencode 与 ZCode 原生都是按首条消息生成，非扫描仓库描述。
- 调整选项：A 不动（推荐，重启/升级桌面版观察）；B 显式 titleGenerationEnabled:true；C hook 调 LLM 生成写库（title_source='custom'，需防并发写冲突）；D 改 vendor 打包产物（升级即丢，不推荐）。
- 调研文档：`docs/2026-08-16-ZCode会话标题自适应生成调研.md`。

### 开启确认（用户：「这个功能给开启吧，我也不知道是否开启了」）

- **结论：默认已开启，无需且无处配置**。内核语义 `titleGenerationEnabled===!1?{enabled:!1}:{}`——仅显式 false 才禁用；该字段是**会话启动参数**（session start config），不是 cli config.json 顶层配置项（CLI bundle 0 处读取，写入无效）。
- 桌面版唯一显式关闭路径 = `f.automationId?{titleGenerationEnabled:!1}:{}`（自动化/定时任务才关，合理）；普通交互会话默认开。
- 生效证据：本会话标题自动生成（「创建 A 秀实现自适应标题生成」）；CLI 今日 104 条 generated；桌面版 08-15 亦有生成成功例（sess_c462e1c8 等）。
- 零改动（未写任何配置）；历史 first_input 会话无补生成机制（如需可走 hook 方案，未实施）。

## Gitee 看板增强 2:项目联动 + 行信息丰富(2026-08-16 晚,验收迭代 2)

- 用户反馈:「不能联动所有项目」——GitHub 有项目(仓库)选择器可多选,列表联动;GitHub 行有作者/标签/负责人头像等信息。
- Gitee API 实测(公开仓库 oschina/git-osc):issue 有 assignee(login/name/avatar_url)+ user + labels;pull 有 assignees 数组 + user + labels。
- 实施(未提交):① GiteeAccountItem 加 authorLogin/authorAvatarUrl/assigneeAvatarUrl/labels(GiteeLabel{name,color}),client 两处映射补齐;② 自建项目多选下拉(Popover+Checkbox,不复用 TaskProjectSourceCombobox——它绑定本地 worktree Repo 结构,Gitee 是账号仓库):选项从 items 推导 repoFullName 去重,trigger 复用 allProjects key,空集合=全部,联动 filteredGiteeItems;③ 行渲染 GitHub 化:ID 列 GitPullRequest/CircleDot 图标+#number(PR 图标按 state 着色)、标题+非 open 状态 badge+仓库名、副行作者+labels chips(≤3 带色)、负责人列 avatar 圆头像+login。
- 验证:typecheck node/web 全绿、gitee 单测 26/26、oxlint/oxfmt 过、localization 校验过(42 inline defaults 差异为 report-only 遗留)。dev 已重启(窗口弹出)。
- 设计文档:docs/2026-08-16-任务页Gitee看板.md(追加「增强 2」节)。

## ZCode 会话标题：TUI 终端标题实现（2026-08-16 傍晚，接上午调研）

- 用户续问并要 PR：实现 TUI 侧"按内容自动生成标签"（对齐 opencode 的 `OC | <标题>`）。
- 实现完成（zcode-cli 仓库，勿在 orca 仓库找改动）：新增 `packages/zcode-tui/src/session-title.ts` + `test/session-title.test.ts`，`index.ts` +13 行——首条用户消息（displayInput）→ 空白压缩/码点截断 50 字符 → 发射 OSC `\x1b]0;ZC | <标题>\x07`（仅 TTY）；`/new` 重置。
- 验证：bun build ✓ / bun test 502 pass ✓ / typecheck ✓。设计+记忆在 zcode-cli：docs/2026-08-16-会话标题自动生成方案.md、.workbuddy/memory/2026-08-16.md。
- 【认知更新】上午结论"原生已生成标题"指**内核 LLM 标题（写 cli db）**，但 TUI 协议（projection/events）不暴露该字段，终端宿主（Orca/iTerm）标签仍是进程名 `zcode`——所以 TUI 侧 OSC 发射是有价值的最小改动；内核标题接入 = 后续增强（需内核协议加字段）。

## 日历二期：个性化功能（阴历/周末/法定节假日/跨月高亮）（2026-08-16 傍晚，calendar-feature，未提交）

- 需求（用户逐项发，AskUserQuestion 确认粒度）：① 支持阴历=四层全做（节日+节气标注、农历年月增强、农历显示开关、农历日期排期）；② 周末特殊标记（周六日不上班）；③ 法定节假日=完整休/班+调休（用户拍板，非只标节日当天）；④ 跨月高亮当月。
- 关键数据：**2026 官方放假安排 = gov.cn 通知原文 WebFetch 抓取**（元旦 1/1-1/3 补班 1/4；春节 2/15-2/23 补班 2/14、2/28；清明 4/4-4/6；劳动节 5/1-5/5 补班 5/9；端午 6/19-6/21；中秋 9/25-9/27；国庆 10/1-10/7 补班 9/20、10/10）。2025 为公开事实。2027 待发布逐年补。
- 新增文件：solar-term.ts（24 节气分钟偏移算法，基准 1900-01-06 02:05 UTC）、festival.ts（10 农历节日+除夕+getDayLabel 优先级 节日>节气>农历日）、holiday-data.ts（休班例外表+getDayStatus）、测试 ×3；扩展：lunar-date.ts（getLunarYearInfo/lunarToGregorianDate/getLunarYearName 干支/getLunarYearMonthDayText/导出 LUNAR_MONTH_NAMES）、shared calendar-types（CalendarEntry.lunarRepeat 必填可空字段+校验）、calendar-time.ts（lunarRepeatDateKey/collectEntriesByDateKey）。
- UI：网格（周末 bg-muted/40 底纹+补班日移除、休/班徽标 [休=text-destructive、班=muted]、右上角节日>节气>农历日、跨月 bg-muted/20+chips opacity-60、表头六/日红字、lunarRepeat 按 viewYear 分桶）、日面板（农历年月行+休班徽标+节日节气，**顺带修复既有 bug：原来不按选中日过滤条目**）、侧栏（显示农历 Checkbox→GlobalSettings.showCalendarLunarInfo 默认 true + 迷你月历周末红字）、对话框（「农历日期（每年重复）」Switch+月/日 Select，锚点 date=当年公历对应日，腊月三十不存在的年份回落 today）、CalendarPage（useAppStore 读设置+updateSettings 切换）。
- 【坑-重要】农历反推 lunarToGregorianDate 闰月 bug：目标为闰月时月份循环必须含同名普通月（m < month+1），否则 2025 闰六月差 30 天——roundtrip 单测抓出。
- 【坑】CalendarEntry 加必填字段 lunarRepeat 后，既有测试 fixture 全部缺字段 → tsc 报错逐个补 lunarRepeat: null。
- 【坑】JSX 大括号条件分支插入时漏闭合 `)}` → typecheck:web 报 TS1005。
- 验证全绿：三 typecheck、日历单测 53/53（节气已知日期/反推 roundtrip 含闰六月/休班表/分桶/除夕）、oxlint 0、oxfmt、localization catalog（zh 11857 键 42 日历键一一对应）/extraction/coverage。
- 设计文档：docs/2026-08-16-日历二期个性化功能设计.md（含约束核对表+实施记录+验收方式）。
- 【提醒】calendar-feature 分支仍零提交；dev 实例当前是 /tmp/orca-gitee-accept（Gitee 验收 worktree）占用 5173，日历验收需停它换 calendar-feature 实例。

## 移动端探索 + 自建服务器中转方案（2026-08-16 晚，纯调研未动代码）

- 需求：探索 Orca 手机 App（安卓）如何使用/设计/安装/配对；评估把数据经用户自己服务器中转的改造（用户有一台公网服务器）。
- 架构结论：mobile/ = React Native(Expo) 配套 App，功能=远程遥控桌面（终端/worktree/任务/PR 审阅/浏览器/语音转写）；**手机不存业务数据，全部 RPC 从桌面拉**。配对=桌面 Settings→Mobile 生成 QR（`orca://pair?code=` base64url JSON 配对要约 v2：endpoint/deviceToken/publicKeyB64/pairedDeviceId/scope + 可选 relay 邀请）；桌面 WS 服务端口 6768（默认 loopback，首次配对扩 0.0.0.0，STA-2370）；鉴权=应用层设备令牌 + E2EE（tweetnacl，RN 无法固定自签证书故不用 TLS）；移动端 RPC 白名单 ~250 方法（MOBILE_RPC_METHOD_ALLOWLIST，src/main/runtime/runtime-rpc.ts）；双通道=直连 ws://ip:6768（LAN/Tailscale）或官方 Relay（director+cell 两层，双方都主动出站连服务器，QR 含 10 分钟有效 inviteToken；生产 relay.onorca.dev，可用环境变量 ORCA_RELAY_URL 覆盖 director）。
- **关键结论：自建中转=零代码改动**。路径 A（推荐）：frp 反向隧道（服务器 frps + 桌面 frpc 出站映射 127.0.0.1:6768）+ 桌面 Settings→Mobile 自定义地址填 wss://自己域名:端口 → QR 自动通告该地址（resolveAdvertisedPairingEndpoint 支持任意 ws(s):// URL，MobilePane.tsx:186 已接 custom address）；鉴权在应用层所以透明转发即可，手机端 app/h/[hostId]/edit.tsx 也能手动改 host 地址。Android usesCleartextTraffic:true 明文 ws 可用但建议 wss（否则设备令牌可被中间人看到重放；wss 后服务器只见加密流量，E2EE 密钥只在手机与桌面）。路径 B（自研兼容官方 Relay director+cell）=大工程（服务端不在仓库，协议客户端侧在 shared/mobile-relay-phone-protocol.ts + relay-control-protocol.ts 可逆向）；路径 C（服务器复刻 Orca RPC 服务端当数据源）=手机零改动、服务器实现 E2EE v2+deviceToken+JSON-RPC 子集。
- 产出：`docs/2026-08-16-移动端探索与自建中转方案.md`（含 7 条约束清单 + 三路径对比表）；本日记忆。未动代码。
- 待办：等用户拍板路径（A 需公网 IP/域名/证书前提确认）。

## Gitee 详情对话框 + 架构决策(2026-08-16 深夜)

- 用户拍板:**copy 路线**(Gitee UI 完全独立重写,不复用 GitHub 渲染组件)——上游日更 66 commits,复用=每次上游改 GitHub 渲染组件都要跟修适配层+合并冲突,被动受影响;copy=零耦合,代价仅是按需手工对齐视觉。落地原则:Gitee 界面全部收敛独立文件(GiteeItemDialog 已独立;TaskPage gitee 分支后续抽 GiteeTasksBoard,TaskPage 只留挂载点)。
- 详情对话框(浏览版)完成:点 Gitee 看板行 → GiteeItemDialog(头部图标+编号+状态+作者/负责人/时间+打开网页按钮;页签:对话=body+评论、文件=PR files±行数+patch 预览、提交=PR commits)。数据:4 个新 IPC(gitee:itemDetail/itemComments/pullFiles/pullCommits),client 4 个新函数,端点在 swagger 实测确认(issue/pull 详情、comments、files: filename/additions/deletions/status/patch、commits: sha/commit.message/author)。
- Gitee API swagger 全量分析(175 端点)已归档设计文档:**merge/review/assignees/labels CRUD/milestones/comments/commits/files/check-runs 全都有**——1:1 能力边界:仅 GitHub 服务端搜索 qualifiers(实测 /search/issues 只全文)与 GraphQL 不可复制,其余可做。
- 【坑】requestGiteeJson 泛型传联合类型时 result.data 不自动收窄,须 as 断言;cn 的 import 路径是 '@/lib/utils' 不是 '@/lib/cn'。
- 验证:typecheck node/web 全绿、gitee 单测 26/26、oxlint/oxfmt 过、sync:localization-catalog 补 GiteeItemDialog 9 个新 key;main bundle 已确认含 4 个新 handler。dev 已重启(worktree /tmp/orca-gitee-accept)。
- **验收前置**:用户 77 个 Gitee 仓库 PR/议题均为空(实测)→ 列表空,点不了行;验收详情对话框需先在 gitee.com 任一仓库建测试 issue。
- 设计文档:docs/2026-08-16-任务页Gitee看板.md(含 swagger 能力矩阵 + 详情对话框方案)。

## 提交 + PR(2026-08-16 深夜)

- 提交:worktree /tmp/orca-gitee-accept 内 commit 4a4686049(形态对齐+项目联动+详情对话框,8 文件 +1127/-74,排除 node_modules 符号链接)。
- 推送:origin(GitHub fork penghuizhang/orca)custom + gitee-integration 两个分支。**【坑】GitHub 直连被墙(git push Empty reply)、Clash 7890 代理 CONNECT aborted、33331 是 API 端口;唯一可用 = macOS 系统代理 54687(`git -c http.proxy=http://127.0.0.1:54687 push`)。gh CLI 正常(走系统代理)。**
- PR:#1(gitee-integration → custom,12 提交 +4082/-66,OPEN):https://github.com/penghuizhang/orca/pull/1
- PR 合并后:主工作目录 checkout custom 即含全部 Gitee 代码;worktree /tmp/orca-gitee-accept 待用户验收完删除;dev 实例仍在跑。

## 日历二期验收微调 2：连休节假日横条（2026-08-16 傍晚）

- 反馈：连休（国庆 10/1-10/7）每格标「休」看不出连休几天、不知道哪个节日（参考截图：黄历 App 跨格横条显示「国庆节 (休)」；截图经智谱视觉模型读取）。
- 实现：holiday-data.ts 加 HOLIDAY_BLOCKS 段表（2025/2026 共 13 段，段名显式：元旦/春节/清明节/劳动节/端午节/中秋节/国庆节）+ getHolidayBlock(dateKey)→{name,totalDays}；网格段首格子渲染跨格横条（absolute，width=calc(N*100%+(N-1)px)，N=行内连续格数，跨行段按行拆分），文本「国庆节 · 7天」（holidayDays i18n 复数变体 en one/other/zh）；段内格子 pt-5 让位；补班日无横条；纯周末无横条。
- 【坑】i18next 复数变体键（holidayDays_one/_other）不满足 verify-localization-extraction（代码引用基础键 holidayDays）→ 必须同时保留基础键。
- 验证：单测 55/55、typecheck:web、oxlint 0、oxfmt、localization 三检查全绿；dev 已重启。

## 官方 Gitee 图标(2026-08-16 傍晚,commit fd3516ba1 直推 custom)

- 用户反馈:任务页来源图标条里 Gitee 用的 lucide FolderGit2 文件夹图标不是官方的。
- 官方图标来源:**https://gitee.com/logo.svg**(200,红圆 #C71D23 + 白色 G path,viewBox 0 0 89.7 89.7);favicon.ico(64px)备选;官网 head logo 在 e-assets.gitee.com。
- 新组件 src/renderer/src/components/icons/GiteeIcon.tsx(官方原色版,参照 JiraIcon 模式);替换 3 处 FolderGit2:task-page-localized-options(来源图标条)/SidebarTaskNavButton(侧边栏 hover)/TasksPane(设置卡片);TaskPage 项目选择器 trigger 的 FolderGit2 保留(语义是项目选择非品牌)。
- 验证:typecheck web 过、oxlint/oxfmt 过、CDP DOM 确认 data-task-source=gitee 按钮内 circle fill=#C71D23。
- 提交 fd3516ba1 直推 origin/custom(未走 PR,小改动;用户后续若要 PR 流程再调整)。

## 日历二期验收微调 3：横条布局挤压修复（2026-08-16 傍晚）

- 现象（截图+视觉模型确认）：横条「中秋节 3 天」覆盖日期区域，右上角小字与横条重复，格子内容挤。
- 根因：`cn('py-1', block && 'pt-5')` 依赖 tailwind-merge 复合类部分覆盖（py-1 与 pt-5 冲突处理不可靠），pt-5 可能被吞 → 日期行仍在顶部被横条盖住。
- 修复：① padding 改三元 `block !== null ? 'pt-4 pb-1' : 'py-1'`（绕开 twMerge）；② 段内格子右上角 dayLabel 隐藏（横条已含节日名，避免重复）；③ 横条 h-4→h-3.5、top-1→top-0 贴顶，段内 pt-4（16px）→ 横条 0-14px、日期行 16px 起 2px 间隙。
- 验证：typecheck:web、oxlint 0、oxfmt、coverage 过；dev 已重启。
- 【坑-可复用】cn() 里避免 py-* 与 pt-*/pb-* 混用依赖 twMerge 合并，用三元显式切换。
- 【待用户拍板】2027+ 节假日数据更新机制（代码内维护 vs 数据文件化 vs 应用内编辑 UI）。

## 图标改动改走 PR(2026-08-16 傍晚)

- 用户要求图标改动也走 PR:回退 custom 的直推(fd3516ba1 → force push 回 35bf9fe0c),新建 feature 分支 gitee-official-icon(含 fd3516ba1),推远端,创建 **PR #2**(gitee-official-icon → custom,+28/-6,OPEN):https://github.com/penghuizhang/orca/pull/2。
- 【坑】直推后要改走 PR:force push 回退共享分支(单人项目可接受);worktree 切分支(gitee-official-icon)后 dev 仍在跑,代码内容不变(同一提交)。
- 后续工作流:功能改动一律 feature 分支 + PR → custom,不再直推 custom。

## 项目选择器「没有项目」修复(2026-08-16 傍晚,PR #3)

- 用户反馈:项目选择器显示「没有项目」,但账号有 77 个仓库。
- 根因:选择器选项 `giteeRepoOptions` 从聚合行(giteeItems)推导,仓库无 PR/议题 → 空 → 「没有项目」。
- 修复:TaskPage gitee 加载 effect 并行拉 `gitee:listRepos`,选项改为账号仓库 fullName 列表(与数据无关,始终显示全部仓库);过滤逻辑不变。
- 验证:CDP 确认选择器列出 bunny-baby/* 全部仓库、「没有项目」消失;typecheck/lint 过。
- 提交 658df3259,分支 gitee-project-picker-fix(基于 custom,与图标 PR #2 隔离,stash 搬运),**PR #3**:https://github.com/penghuizhang/orca/pull/3

## 研发云集成：调研完成（2026-08-16 深夜，未动代码）

- 需求（用户）：基于 custom 继续研发云集成分析，照 Gitee 方式集成，且「研发云会集成得更多」（全链路 DevOps）。
- **重大突破**（全部实测）：
  - 平台=天翼云 EasyCoding 企业内网部署（www.srdcloud.cn 解析 10.158.231.10，非公网）；env 标签 hasGongzuoxiang/hasDoc/hasVersionMgnt/hasLowCode
  - **git 托管=Gerrit 2.13.9-CCM-20200428（code.srdcloud.cn）**，SSO 会话认证（Basic 无效），平台可签发 Gerrit HTTP 密码（app-inf/v1/queryUserGerritLoginInfo）+ SSH 密钥管理（auth/v1）
  - **代码中心=/api/codebackend/codecenter/{mod}/v{n}，383 端点**（前端 bundle 提取）：repository/prreq(PR 全生命周期)/gitweb(浏览)/branch/protect-branch/app-inf(含 tokens)/management-backend(提交↔工作项关联)/ext-inf(getWorkItems)/aireview(AI评审)/ide/auth/config/recycle/repo-metric 等 30+ 模块
  - **工作项(zxwim)=独立后端**：/zte-rdcloud-rdc-rdcserver/rdc + /zte-rdcloud-rdc-wimbackend/wic/api/workspaces/{ws}/work_items；**鉴权=X-Emp-No(工号)+X-Auth-Value(UAC token)** 自定义头（免登录实测返回 0002）；SSO=中兴 UAC（uactest.zte.com.cn, businessSystemCode=100000462184），config env=beta
  - 应用清单：GET /api/portalbackend/portal/webconfig/apps **免登录 200，85 应用**（ci→/api/cibackend、artifact→/api/artifactbackend、review/version/taas/sdm/sqsc/imds/deploy/低代码×4/API网关×2…）
  - 认证四套：门户天翼账号 SSO(oauth_access_token)→网关会话路由；工作项 UAC→X-Emp-No/X-Auth-Value；acbackend→apiKey+userId+sessionId 头（**平台有 API key 管理** /users/apikey，实测路由 200 但用户 codefree key 仅 codechat 有效）；Gerrit→门户会话
  - **无公开 OpenAPI**（swagger 501/SPA fallback）；网关未知路由 501、缺鉴权 406「鉴权字段未传或鉴权路由不匹配」→ 406=路由存在
  - CodeFree=改装版 opencode（codefree-o 二进制 82MB bun 编译）：sso-oauth 认证、codechat/completions、mcpconfig（**平台同步 MCP 服务器配置**）、skillmarketplace（技能市场——用户以为的「MCP 技能」在这）、modelmgr/statserver/usermanager
- **方案文档**：docs/2026-08-16-研发云集成方案.md（L0 识别 1 天 / L1 认证+工作项+PR 只读 2-3 周 / L2 评审操作·工作项操作·CI·制品·AI评审 / 11 条约束清单）
- 关键差异 vs Gitee：双后端双鉴权（codebackend 会话 + 工作项 X 头）；内网可达性（SSH 远端不可用→优雅降级）；Gerrit 项目名单段无 owner/repo；优先 prreq API 不直连 Gerrit REST
- 待用户拍板：范围 L0/L0+L1、令牌形态（会话 token 粘贴 vs 探 API key 体系）、工作项按 workspace 还是 view 组织、L1 是否含 CI/制品、入口形态
- 【坑-可复用】zsh 的 UID 是只读保留变量（赋值报错）；网关 406 文案=「认证失败…鉴权路由不匹配」是路由级鉴权提示不是参数错误

## 研发云方案补充：独立页签 + token 生命周期（2026-08-16 深夜，未动代码）

- 用户追问：① 能不能像日历一样单独创建「研发云」页签；② 平台 token 有效期短每天都要登录，怎么处理。
- **① 独立页签=成立**，日历 TopLevelView 机制就是范式（类型+Record 穷举+SidebarNav+AppWorkspaceShell+ui.ts+i18n+14 枚举点）；研发云比任务页更适合独立页签（多模块 DevOps 全家桶）。SrdcloudWorkspacePage 结构=顶部连接状态卡（token 有效期/一键重登）+页签（工作项/代码仓库/PR，二期 CI/制品）；copy 路线独立文件；任务页 Tasks 来源为二期可选项。
- **② token 痛点根因实测确认**：~/.codefree-cli/oauth_creds.json 显示 access_token expires_in=86400（24h）、token_type=bearer、**refresh_token_expires_in=0（平台不签发 refresh token）**→ 每日登录是平台强制。
- **分层凭证策略**：git 通道用 Gerrit HTTP Password（用户自助生成，长期，一次搞定）；API 通道用门户/UAC access_token（24h）+ 过期检测；**一级处理=一键 OAuth 重登**（开系统浏览器→SSO→回跳 localhost 自动收 token，每天点一次 5 秒不手输）；二级=后台自动续期（需实测平台有长效凭证才可，现状证据不支持）；三级=向平台组申请企业凭证/开放 refresh_token（长期解，用户在公司可推进）。
- token 安全：safeStorage 加密、不进日志/聊天、env 优先、按通道分存互不影响、有效期倒计时提醒。
- 待实测（L1 开工第一件事）：门户 OAuth 完整响应结构（用户真实会话抓包确认 refresh 有无/网关头名）、acbackend apikey 能否覆盖 DevOps 服务、Gerrit HTTP password 自助生成路径。
- 设计文档已更新：docs/2026-08-16-研发云集成方案.md 新增第 7 节（独立页签形态）+第 8 节（认证与 token 生命周期），决策点扩到 6 项（新增：入口形态、认证方案、git 通道）。

## 图标"没看到"原因与重建(2026-08-16 傍晚)

- 用户合并 PR #2(图标)后 dev 里没看到官方图标。
- 根因:dev worktree 当时在 gitee-project-picker-fix 分支(基于图标合并前的 custom 拉出,不含 fd3516ba1)。
- 处理:worktree 切回 custom + reset 到 origin/custom(283d1be58,PR #2 图标 + PR #3 选择器修复都已合并)+ 重启 dev。
- 验证:CDP 确认 data-task-source=gitee 按钮内 circle fill=#C71D23(官方图标已渲染)。
- 【教训】多分支并行时,dev 实例跑的是 worktree 当前分支的代码;合并远端 PR 后必须 fetch + reset 到 origin/custom 再重启,否则看到的还是旧分支构建。

## 研发云集成：用户决定搁置（2026-08-16 深夜，未动代码）

- 用户看完 token 生命周期方案后决定：**不集成研发云**，「很麻烦，还是在页面上操作」，先暂时搁置。
- 归档：调研成果全部保留在 docs/2026-08-16-研发云集成方案.md（含独立页签形态、分层凭证策略、383 端点清单、认证四套体系），日记忆本条；未来若恢复，从该文档直接续接（方案已完整，缺的只是实测确认项：门户 OAuth 响应结构/acbackend apikey 覆盖范围/Gerrit HTTP password 路径）。
- 本次任务零代码改动（纯调研+文档），符合「设计先行」流程；无约束清单需要核对（未实施）。
