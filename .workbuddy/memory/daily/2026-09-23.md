---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '5e1c9a37-77d4-4e5b-9f2a-3c8d61b40e7f'
  PropagateID: '5e1c9a37-77d4-4e5b-9f2a-3c8d61b40e7f'
  ReservedCode1: 'a1f0d2c4b6e84917a3c5d7e9f1b2a4c6'
  ReservedCode2: 'b2c3d4e5f6a74819b0c1d2e3f4a5b6c7'
---

# 2026-09-23

## 任务：切换 custom、清理已合并分支、拉上游合并并打包

### 需求
用户：切换到 custom 分支，删除当前已合并过的分支；拉取最新上游源码，进行合并打包。

### 结果一句话
**全流程完成：切到 custom（ff 到 PR #23 合并点）、删掉已合并的 feat/notes-editor-fix（本地+远端）、强拉上游 304 提交合并进 custom（2 冲突叠加解决 + 1 处无冲突集成缺口 typecheck 兜出）、6 语言键位三向核对零丢失、全部门禁绿、打包安装 `1.4.208-local.1790130615259.c8f75151e828`（构建号基数已到 v1.4.208）——中间返工 2 次：mobile 独立 workspace 未装依赖、.DS_Store 撞 buildId 校验，两坑均已蒸馏。**

### 关键决策

1. **`.temp/` 移出仓库而非删除**：未跟踪文件会阻塞 verify-features（只豁免 `.codegraph/`）；内容是截图/lint 输出，移至 `/tmp/orca-temp-backup-20260923/` 留底。
2. **悬空的 `.oxlintrc.json` 改动随本次入库**：`global-settings-types` max-lines 豁免调至 320 是 notes 功能遗留的未提交改动，不入库工作区永远不净。
3. **本地 feat 分支哈希与远端不同仍可删**：PR 经 Git Data API 重建过提交（哈希不同），但 `git diff feat origin/custom` 为空 + `git cherry` 等价 ⇒ 内容零差异，`-D` 安全删除。
4. **2 冲突全按「双方各新增」叠加**：`agent-catalog.tsx`（二开 zcode 图标分支 + 上游 opencode2 分支）、`en.json`（`zcode_label` + `opencode2_label` 邻接键，给先出现方补逗号保闭合）——严格按冲突手册判定，未用 `--ours/--theirs`。
5. **`hasPartialCost` 集成修复是 typecheck 兜出来的、不是冲突**：上游给 `UsageProviderOverview` 加必填字段，二开 zcode/pi 的 createProvider 不在冲突文件里但缺字段 ⇒ **教训：无冲突合并 ≠ 类型集成完成，pnpm tc 是硬门禁**。取值按上游对「无 unpriced 概念 provider」的先例 `false`。
6. **changed 门禁在大合并后失真，改定点 lint**：`check:code-quality:changed` 以 `origin/main` 为基线，合并上游后变更集=10173 文件（全库），3184 个 finding 全是存量/上游噪音（native-chat restyle 等）。按「不给上游跑重排版」原则，对本次自改 3 个源文件跑齐 5 套 oxlint 配置（root/design-system/casting/react-doctor/type-aware）全部 0/0。

### 改动文件
- 合并提交 `f02c056817`（Merge upstream/main 1b85be67d8，304 提交）
- 冲突解决 2：`src/renderer/src/lib/agent-catalog.tsx`、`src/renderer/src/i18n/locales/en.json`
- 集成修复 2：`src/renderer/src/components/stats/usage-provider-normalization.ts`（zcode/pi 各 +`hasPartialCost: false`）、`usage-overview-model.test.ts`（fixture 补 zcode/pi 零值）
- 入库悬空改动：`.oxlintrc.json`（max-lines 320 豁免）+ 记忆 2 篇
- 提交序列：`1b7c8a0f93`（lint 配置）→ `5fb0a55d4d`（记忆）→ `f02c056817`（merge）→ `71cbf8bded`（集成修复），全部已 push origin/custom
- 备份 tag：`backup/pre-sync-2026023`（已推 origin）

### 归档路径
- 设计/影响说明：`.workbuddy/docs/workflow/2026-09-23-切分支删合并分支与上游同步打包说明.md`
- 本日记忆：`.workbuddy/memory/daily/2026-09-23.md`

### 验证
- `pnpm install` 16.4s（锁文件有变动，已重装）；`pnpm tc` exit 0；受影响单测 16/16（notes 10 + usage 6）
- `verify-features.mjs` 全绿「可以打包」；工作目录干净
- 6 语言键位三向对照：二开新增 en 250/zh 207/es 73/fr 28/ja 73/ko 73 **全部零丢失**，上游新增键亦零丢失
- 冲突标记全库残留 0；en.json `json.load` 通过
- 定点 lint：3 文件 × 5 配置 = 全 0 warnings 0 errors

### 约束核对表

| 约束 | 结果 | 证据 |
|------|------|------|
| 浅克隆 fetch 后核日期 + ls-remote 对照 | ✅ | upstream/main `1b85be67d8` 2026-09-22 ≡ ls-remote |
| 冲突禁止 --ours/--theirs 一把梭 | ✅ | 2 冲突均按「谁改的」判定：双方各新增→叠加 |
| en.json 邻接冲突保闭合 + 三向键对照 | ✅ | json.load OK；6 语言零丢失（上方数字） |
| merge commit --no-verify | ✅ | `git merge --no-edit --no-verify` |
| 工作区全净才能打包 | ✅ | `.temp/` 移出 + 悬空改动入库，status 空 |
| 构建号基数 ≥ 远端最新 tag | ✅ | 补 `v1.4.207`+`v1.4.208`，基数=1.4.208 |
| 锁文件变动则 pnpm install | ✅ | package.json/lock 有 diff → install 16.4s |
| 永久分支不删 | ✅ | 仅删 feat/notes-editor-fix（本地+远端），custom/main 保留 |
| 合并后 tc + verify-features | ✅ | tc exit 0 + verify 全绿；另兜出 hasPartialCost 缺口 |
| 重启 orca-s 交用户手动 | ⏳ | 会话在 orca-s 内，装完待用户手动重启 |
| .workbuddy 入库 git add -f | ✅ | 两提交均 -f，已 push |

### 打包结果（第 3 次成功）
- 版本 **`1.4.208-local.1790130615259.c8f75151e828`**（基数 1.4.208 = 远端最新 tag，preflight 两个 ❌ 全消）；产物 `dist/orca-s-1.4.208-…-arm64-mac.zip`（212M）+ `orca-s-macos-arm64.dmg`（211M，2026-09-23 10:38）
- `codesign --verify --deep --strict` exit 0；`mobile-web bundle` 15 路由可复现 buildId 校验过
- **磁盘已是 1.4.208，运行中仍是 1.4.206**（`ps aux | grep daemon-entry.js`）：安装成功≠升级生效，且本会话跑在 orca-s 终端内 ⇒ **重启 orca-s 交用户手动执行（Cmd+Q 后重开）**
- 返工 2 次：① 上游新 `build:mobile-web` 链路需 `cd mobile && pnpm install`（419 resolve 错）② `mobile/{src,app}/.DS_Store` 撞 buildId CRLF 校验（gitignored 垃圾文件绕过干净检查但躲不过字节校验）

### 经验沉淀（建议蒸馏）
- **changed 质量门禁的基线是 origin/main**：custom 合并上游后本地跑它=全库扫描+存量噪音，不可作门禁；正确做法=对自己改动的文件跑 5 套 oxlint 配置矩阵。已补进冲突手册附注。
- **上游给共享类型加必填字段**：症状=tc 报 TS2741/TS2739 且报错文件可能根本不在冲突列表；按上游同类型其他成员的取值先例补。
- zsh 循环坑：`for f in $FILES` 不分词（SH_WORD_SPLIT 关），要用 `${=FILES}` 或直接内联列表。

## 任务：上游同步第二轮（26 提交）走 PR 合入 custom（PR #24）

**需求**：拉取上游最新代码 → 合并 → 创建 PR 合并到 custom（首次放弃直接 merge custom，改走 PR 留痕）。

**决策与过程**：
- 强拉 `upstream/main` 至 `ae3d380b23`（refspec + 核日期 = 远端 tip）；补 `v1.4.209` tag；从 custom 切 `sync/upstream-ae3d380b23` 分支合并。
- 34 冲突五类全解：①usage provider 注册链（zcode/pi ↔ Muse，叠加）②agent 注册表（叠加）③stats 面板（else-if 链/按钮/tab 手工合并）④i18n 五语言（**三向深合并** stage1/2/3，二开键零丢失、零叶值冲突）⑤startup（叠加）。23 个 TS/TSX 用逐块策略脚本（67 块：auto 叠加 / 取上游 / 同语句整体重构）。
- **同语句双版本**文件手工重构：`usage-provider-slices`（保留二开 factory 重构 + 补 Muse 条目与 `MuseUsageShape`）、`usage-provider-normalization`（zcode/pi/muse 三函数完整重建，公共后缀闭合 muse）。
- 集成修复（tc/门禁抓出的非冲突缺口）：
  - 上游新增 Muse 测试缺 `zcode/pi` 必填字段 → 补（**tc 是硬门禁的又一次验证**）；
  - 二开存量：`UsageShape`→`UsageContract` 改名 34 处（anti-slop 新规则视角）、calendar `target: object`→`CalendarPersistence`（兄弟 `installXxxContext` 先例）、notes 常量独立 `notes-root-directory.ts` 断 `constants↔default-global-settings` 环、zcode/pi 重复导入合并、code-navigation 3 处文案本地化 + 六语词条；
  - localization 四门禁存量：`sync:localization-catalog --fix`（+9 advanced.search 键）、`sync:localization-runtime-catalog --fix`（重生成）、coverage 词条补齐后 **四门禁全绿**。

**改动文件**：34 冲突文件 + `usage-slice-factory.ts`（改名）+ 新增 `src/shared/notes-root-directory.ts` + `code-navigation-bindings.ts` +6 locale + `en-runtime-required.json` + `.workbuddy/docs/workflow/2026-09-23-上游同步26提交PR合并说明.md`。

**验证**：`pnpm tc` ✅；oxlint ✅；anti-slop ✅（36→0）；type-aware/reliability/ratchet×4/readme/rpc/skill×2 ✅；**localization 四门禁 ✅**；`check:code-quality:changed -- <custom tip>`（= pr.yml 门禁，correct baseline）✅ 0 findings/276 文件（SAFETY 注释按上游先例逐行补在 zcode/pi 断言上）；触碰面单测 13 文件 79 用例 ✅。`audit:code-quality:native` 仅剩 **9 个上游 mobile 存量警告**（`1b85..ae3d` mobile 零改动=两侧同态，pr.yml 不跑此步、mobile.yml 在 mobile 工作区跑裸 oxlint 不受阻）。

**根因（存量为何此刻爆）**：此前每次同步都直接 merge custom、不走 PR ⇒ pr.yml 的门禁（changed + 4×localization）从未在二开存量上跑过；首次走 PR 全部暴露。门禁脚本本身 `1b85..ae3d` 零改动。

**归档路径**：设计/影响说明 `.workbuddy/docs/workflow/2026-09-23-上游同步26提交PR合并说明.md`（含五类冲突表与约束清单）；本节日记忆；冲突解法细则见 `.workbuddy/memory/pitfalls/orca-merge-upstream-conflict-playbook.md`。

**经验（建议蒸馏 M）**：见下条 MEMORY.md 蒸馏「PR 门禁地图」。

### 约束核对表

- 【约束】浅克隆强拉 + 核日期 = 远端 tip → ✅ `ae3d380b23` 与 `ls-remote` 一致
- 【约束】不改根 package.json 版本号 → ✅ 本轮根 package.json 双方零改动，未碰
- 【约束】`pnpm tc` 硬门禁 → ✅ 通过（且真抓出上游新测试缺 zcode/pi 的集成缺口）
- 【约束】i18n 三向键位核对、二开键零丢失 → ✅ 脚本断言 LOST-KEYS=0、LEAF-CONFLICT=0
- 【约束】PR 只允许 base=custom、禁止往 main/master 提 → ✅ PR #24 base=custom
- 【约束】changed 门禁基线失真 → ✅ 改用 `-- HEAD^1`（custom tip）本地预演 = CI 同款
- 【约束】`.workbuddy` gitignore 需 `git add -f` → ✅ 文档/记忆均 `-f` 入库
- 【约束】工作区先清遗留再开合并 → ✅ 先提交上会话遗留 MEMORY.md 蒸馏条目（`9ad41ea559`）
- 【坑重踩】`cmd | tail` 管道吃退出码 → ⚠️ **本轮又踩一次**（oxlint 结果被 tail 吞成 exit0/127），已改为重定向文件再判 `$?`；与 2026-09-23 打包坑同源，保留原条目并本节佐证
