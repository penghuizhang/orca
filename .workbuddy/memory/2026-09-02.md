# 2026-09-02 工作日志

## 任务：移除 Gitee 集成

**需求**：用户决定不再集成 Gitee，要求完全移除 Gitee 相关代码。

**影响评估完成**：
- 18 个 Gitee 专属文件（~2,489 行）可直接删除
- ~30 个引用文件需修改
- en.json 6 组 Gitee i18n key（~38 行）
- .oxlintrc.json 1 处豁免规则

**设计文档已产出**：`docs/2026-09-02-移除Gitee集成影响说明.md`

**当前状态**：已实施完成，typecheck/oxlint/build 全绿

**【约束】清单**：
- 【约束】删除前必须完整盘点所有引用点，避免遗漏导致编译错误 ✅ 已完成
- 【约束】`src/shared/task-providers.ts` 中 `'gitee'` 是联合类型成员，删除后所有引用此类型的地方都需要更新 ✅ 已完成
- 【约束】switch/case 中的 `'gitee'` 分支必须全部处理，否则 TypeScript 可能报 unreachable code 警告 ✅ 已完成
- 【约束】i18n key 删除后需验证其他语言文件（zh/es/ja/ko）是否受影响 ✅ 已完成
- 【约束】测试文件中的 `'gitee'` 引用必须同步清理 ✅ 已完成
- 【约束】保留 docs/ 下的历史文档（已在 .gitignore 不影响提交）✅ 已完成

**经验**：
1. Gitee 集成采用 copy 架构（独立重写不复用 GitHub 渲染），代码完全收敛到独立文件，删除风险低。IntegrationsPane.tsx 中也需要移除 GiteeIntegrationCard 引用。
2. **【重大教训】打包前必须确保 custom 分支包含所有二开功能！** 本次打包发现日历功能缺失，因为 calendar-feature 分支没有合并到 custom。打包必须从 custom 分支，而 custom 必须包含所有已实施的二开功能（日历、zcode agent、迁库、Gitee 删除等）。

**实施完成**：
- 删除 18 个 Gitee 专属文件（~2,489 行）
- 修改 ~30 个引用文件
- 清理 en.json 6 组 Gitee i18n key
- 清理 .oxlintrc.json 1 处豁免规则
- typecheck 通过（退出码 0）
- oxlint 通过（0 findings）
- build 通过（退出码 0）
- 合并 calendar-feature 到 custom（解决 .workbuddy/memory/2026-08-16.md 冲突）
- 重新打包 orca-s（包含所有二开功能）

---

## 任务：日历数据不显示排查与修复

**需求**：用户反馈日历数据不显示，怀疑数据丢失。

**排查过程**：
1. 检查 orca 数据库（`~/Library/Application Support/orca/profiles/local-default/orca-custom.db`）→ calendar_entries 表为空（0条）
2. 检查 orca-s 数据库（`~/Library/Application Support/orca-s/profiles/local-default/orca-custom.db`）→ calendar_entries 表有 29 条记录
3. 检查 orca-data.json 和备份文件 → 均无日历条目（迁移已完成）

**根因分析**：
- `configurePackagedUserDataPath()` 函数已定义（`src/main/startup/configure-process.ts:208`）但从未被调用
- 导致 orca-s 应用使用了官方 Orca 的 userData 路径（`~/Library/Application Support/orca`），而不是自己的路径（`~/Library/Application Support/orca-s`）
- 日历数据实际存在于 orca-s 数据库中，但官方 Orca 数据库为空

**修复措施**：
1. **代码修复**：在 `src/main/startup/main-process-preflight.ts` 中添加对 `configurePackagedUserDataPath()` 的调用
   - 当应用是打包版本且不是开发模式时，将 userData 路径设置为 `orca-s`
2. **数据恢复**：从 orca-s 数据库复制 29 条日历记录到 orca 数据库

**用户新需求**：
- 将数据库文件放到外部目录 `/Users/zhangpenghui/software/sqlite`
- 避免版本更新时丢失数据
- 保证数据不丢失的机制

**设计文档已产出**：`docs/2026-09-02-日历数据库外部存储方案.md`

**当前状态**：已完成实施，typecheck/oxlint/build 全绿

**实施完成**：
1. ✅ 在 `src/shared/global-settings-types.ts` 中添加 `customDbPath` 配置项
2. ✅ 修改 `src/main/persistence/loading-store/calendar-persistence.ts`：
   - 添加 `existsSync` 和 `copyFileSync` 导入
   - 修改构造函数：优先使用用户配置的 `customDbPath`，否则使用默认路径
   - 添加静态方法 `migrateToCustomPath()`：支持从默认路径迁移到外部路径
3. ✅ 修改 `src/main/persistence/loading-store/store.ts`：
   - 在 Store 构造函数中调用 `CalendarPersistence.migrateToCustomPath()` 执行迁移
4. ✅ 更新设置页面 UI：
   - 在 `src/renderer/src/components/settings/AdvancedPane.tsx` 中添加数据库路径配置项
   - 在 `src/renderer/src/components/settings/advanced-search.ts` 中添加搜索入口

**【约束】清单**：
- 【约束】修改 userData 路径配置时，必须在 `initDataPath()` 之前完成，否则会缓存错误路径 ✅ 已完成
- 【约束】数据迁移必须幂等（INSERT OR IGNORE），重复执行不报错 ✅ 已完成
- 【约束】外部数据库路径配置需要考虑路径不存在、权限不足等异常情况 ✅ 已完成（`existsSync` 检查）
- 【约束】数据库备份/恢复需要确保 WAL 日志完整，避免数据损坏 ✅ 已完成（复制 WAL 和 SHM 文件）

**经验**：
1. **【重要发现】configurePackagedUserDataPath() 未被调用**：打包后的 orca-s 应用使用了官方 Orca 的 userData 路径，这是数据"丢失"的根因。修复后需重新打包验证。
2. **数据恢复方法**：使用 SQLite 的 `ATTACH DATABASE` + `INSERT OR IGNORE` 可以安全地从一个数据库复制数据到另一个数据库，幂等且不丢失数据。
3. **外部存储方案**：在 GlobalSettings 中添加 `customDbPath` 配置项，修改 `CalendarPersistence` 支持外部路径，添加自动迁移逻辑。用户可在 Settings → Advanced → Database 中配置。

**修复的类型/lint 错误**：
1. `CalendarPersistence` 使用 `import type` 导入导致不能作为值使用 → 改为普通导入
2. 测试文件中的 `'gitee'` 引用（Gitee 集成已删除）→ 移除引用
3. if-else 语句不符合 oxlint 规则 → 改为三元表达式

---

## 任务：二开工作流程规范化

**需求**：用户反馈同步上游代码后打包的应用二开功能丢失，需要规范化工作流程。

**问题分析**：
1. **功能分支遗漏**：打包时发现日历功能缺失，因为 calendar-feature 分支没有合并到 custom
2. **同步流程复杂**：需要手动执行多个步骤，容易出错
3. **版本号管理不明确**：打包版本号与上游大版本一致，但本地构建版本包含时间戳和 commit hash

**解决方案**：
1. **创建二开工作流程规范文档**：`docs/2026-09-02-二开工作流程规范.md`
2. **设计改进方案**：`docs/2026-09-02-二开工作流程改进设计.md`
3. **验证 custom 分支完整性**：确认所有二开功能已合并到 custom 分支

**验证结果**：
- ✅ 日历功能（calendar-feature）已合并
- ✅ zcode agent 类型（feature/zcode-agent）已合并
- ✅ custom.db 业务库（feature/calendar-sqlite）已合并
- ✅ 浏览器自动化 MCP Server（feat/browser-automation-mcp）已合并
- ✅ 移除 Gitee 集成（remove-gitee-integration）已合并

**当前状态**：已完成文档创建和分支验证，等待用户评审

**【约束】清单**：
- 【约束】打包前必须确保 custom 分支包含所有二开功能 ✅ 已完成
- 【约束】同步上游代码时必须保留二开功能不被覆盖 ✅ 已完成
- 【约束】版本号管理策略明确（基础版本号与上游一致）✅ 已完成

**经验**：
1. **分支结构明确**：main 分支仅同步上游代码，custom 分支包含所有二开功能
2. **打包前检查必须**：打包前必须验证所有功能分支已合并到 custom
3. **文档规范化**：建立标准工作流程文档，避免人为失误

**实施完成**：
1. **创建标准化同步脚本**：`config/scripts/sync-upstream.mjs`
   - 自动同步 upstream → main → custom
   - 验证二开功能完整性
   - 支持 --dry-run 预览模式

2. **创建打包前验证脚本**：`config/scripts/verify-features.mjs`
   - 验证所有二开功能分支已合并
   - 验证关键文件存在性
   - 支持 --full 运行类型检查

3. **更新打包脚本**：`config/scripts/build-orca-s.mjs`
   - 添加打包前自动验证步骤
   - 确保二开功能完整性

4. **创建一键操作脚本**：`config/scripts/orca-sync-and-build.mjs`
   - 一键同步和打包
   - 支持 --sync 仅同步、--build 仅打包、--install 同步+打包+安装

**标准操作流程**：
```bash
# 完整流程（同步 + 打包）
node config/scripts/orca-sync-and-build.mjs

# 仅同步上游代码
node config/scripts/orca-sync-and-build.mjs --sync

# 仅打包（已同步）
node config/scripts/orca-sync-and-build.mjs --build

# 同步 + 打包 + 安装
node config/scripts/orca-sync-and-build.mjs --install
```

5. **创建 orca-dev-workflow Skill**：`~/.agents/skills/orca-dev-workflow/`
   - 触发词：同步 orca、打包 orca、更新 orca、orca sync、orca build
   - 命令：sync-and-build（默认）、sync、build、verify、status
   - 自动验证二开功能完整性
   - 支持 --install 安装到 Applications

**Skill 使用方式**：
```bash
/orca-dev-workflow                    # 完整流程（同步 + 打包）
/orca-dev-workflow sync               # 仅同步
/orca-dev-workflow build              # 仅打包
/orca-dev-workflow build --install    # 打包 + 安装
/orca-dev-workflow verify             # 验证功能
/orca-dev-workflow status             # 查看状态
```
