# 2026-08-18

## 答疑：视觉子代理「余额不足」提示是从哪来的（Q&A，未动代码）

用户看到提示「视觉子代理余额不足，改用智谱直连 API 通道读图」，疑惑：视觉不是托管给 mimo 了吗？

**结论**：视觉从未成功托管给 mimo，该提示是预期现象：
- `read-image` skill 文档写的委托目标 `opencode/mimo-v2.5-free` 在网关**不存在**（Model not supported），文档与实际脱节；
- 用户指定的 `mimo-v2.5-pro` 实测**图片输入被网关拒绝**（404 No endpoints found that support image input），mimo 系均不可用于视觉；
- 实际配置的「视觉子代理」= 把 general-purpose/Explore 覆盖为智谱 `bigmodel/glm-4.6v`（`~/.zcode/v2/agents-state.json` 的 `builtInModelOverrides`），该模型报 429 余额不足 → 提示来源；
- 唯一实测可用视觉通道 = 智谱免费 `glm-4.1v-thinking-flash` 直连 API（base64 + image_url，会话原本用智谱对话接续，从记忆查到通道后改用直连），与已有记忆 [[zcode-multimodal-vision]] 完全一致，**属重踩确认、无新事实**。

**当日核对（2026-08-18 现查）**：`~/.zcode/v2/agents-state.json` 的 `builtInModelOverrides` 仍为 `general-purpose`/`Explore` → `bigmodel/glm-4.6v`，未变 → 之后任何子代理读图仍会撞余额报错。

**待用户拍板**：a) 清空/改掉 override（视觉一律直连智谱 API）；b) 换有余额的视觉模型（记忆记录 glm-4.6v/4.5v/5v-turbo 均 429）。若用户决定改配置，需先出影响说明再动，本次未改。

**经验（T 类，仅流水）**：read-image skill 与 agents-state.json 长期脱节是反复重踩源，但已有记忆条目覆盖（zcode-multimodal-vision.md），无需再蒸馏；skill 文档本身待修（改委托描述为直连智谱流程）——已列入待办，未在本次会话动手。

## 实测：mimo-v2.5 可识图（2026-08-18 下午，推翻旧结论）

用户质疑"mimo 能识图"，实测 opencode 网关（https://opencode.ai/zen/go/v1，`cli/config.json` 的 opencode-go provider）全部 mimo 模型：

- **`mimo-v2.5`（不带后缀）✅ 图片识别成功**：ImageMagick 生成含 `ORCA-818` / `HELLO 12345` 文字的 PNG（/tmp/orca-vision-test.png），模型逐行准确读出。→ **旧记忆 zcode-multimodal-vision.md 结论不全：只测过命名不存在的 mimo-v2.5-free 和拒图的 mimo-v2.5-pro，漏测 mimo-v2.5 本体——这是当前唯一可用的网关视觉通道（无需智谱直连）。**
- `mimo-v2.5-pro` ❌ 400：依然 `404 No endpoints found that support image input`（与旧记忆一致）。
- `mimo-v2-omni` / `mimo-v2-pro` ❌ 400：网关列出但上游 `Unsupported model`。
- 网关 /models 共 26 个模型；mimo 家族 4 个：mimo-v2.5 / mimo-v2.5-pro / mimo-v2-omni / mimo-v2-pro。

**新坑（M 类，待蒸馏）**：urllib 默认 UA 请求该网关 chat/completions 被 Cloudflare 拦（HTTP 403 error code 1010），带 `User-Agent: curl/8.6.0` 即通；/models 用 curl 一直没问题即是此因。

**用户拍板并已实施（2026-08-18 下午）：使用 opencode-go/mimo-v2.5 做图片识别**。变更共 4 项，全部完成并验证：
1. `~/.agents/skills/read-image/SKILL.md`（全盘唯一真实副本，`~/.zcode/skills/` 下无 read-image）委托目标 `opencode/mimo-v2.5-free` → `opencode-go/mimo-v2.5`，description/正文/备选段落同步，补充 mimo 家族可用性说明；
2. `~/.zcode/v2/agents-state.json` `builtInModelOverrides`：general-purpose/Explore 从 `bigmodel/glm-4.6v` → `opencode-go/mimo-v2.5`，根治「视觉子代理余额不足」；cli/config.json 的 subagents.builtInModelOverrides 不生效（记忆确认），刻意未动；
3. **（额外发现）全局 `~/.agents/global-rules/AGENTS.md` 第⑥节也写死了 `model=opencode/mimo-v2.5-free`，已同步改为 `opencode-go/mimo-v2.5`**；
4. auto-memory zcode-multimodal-vision.md + MEMORY.md 索引已更新为「已实施」。

**验证证据**：agents-state.json json.load 通过；三文件 grep 无 `mimo-v2.5-free` 残留；配置组合有效性断言通过（provider.opencode-go.models 含 mimo-v2.5）；对 /tmp/orca-vision-test.png 复测识图成功（读出 ORCA-818）。
**备份**：agents-state.json.bak-20260818 / SKILL.md.bak-20260818。
**约束核对**：【约束1 skill 与实际配置对齐】满足（委托目标=实测可用模型）；【约束2 子代理不再撞余额接口】满足；【约束3 旧记忆结论修正】满足（auto-memory 双处更新）；【约束4 仅改生效位置】满足（cli/config.json 不生效处未动）；【约束5 最小影响】满足（变更均为用户级配置/文档，未触 orca 工程代码）。
**影响说明**：docs/2026-08-18-mimo视觉通道修正.md。
另注：`~/.zcode/skills/` 目录全部为符号链接且没有 read-image，技能列表中的 .zcode 路径为重复登记，勿再按该路径寻找。

## 设计：日历周视图与工时汇总（2026-08-18 下午，设计阶段，未动代码）

### 需求
用户用日历记录每天工作，**周五集中填报工时**到公司系统；痛点是月视图格子小（每格最多 3 条）、无任何工时统计、无导出/复制通道。

### 现状核对（代码事实）
- `src/shared/calendar-types.ts`：CalendarEntry 已有 date/allDay/startTime(HH:mm)/endTime(HH:mm)/category/description → **工时可由时间段直接计算，无需改 schema**；normalize 时 start>end 已并成点事件（时长 0）。
- 存储 `src/main/custom-db/calendar-table.ts`（orca-custom.db v1），IPC `calendar:list/create/update/delete` 纯透传，main 层零改动。
- 渲染三栏：左侧分类筛选+农历+月历缩略图、中间 CalendarMonthGrid、右侧 CalendarDayPanel；工具栏月份导航+今天+新建。
- calendar-time.ts 已有 buildMonthMatrix/formatWeekdayHeaders/collectEntriesByDateKey，周一优先；holiday-data 有 isWeekendDateKey/getHolidayException（班/休徽标）。
- 分类色：meeting=amber、feature=blue、milestone=green、other=zinc；UI 原语有 ui/toggle-group.tsx 可复用做月/周切换。

### 本会话读图方式（T 类流水）
- 视觉子代理（overrides=bigmodel/glm-4.6v）报余额不足 → 智谱 API key 直连成功（`~/.zcode/v2/config.json` provider.builtin:bigmodel-coding-plan.options.apiKey，model=glm-4.1v-thinking-flash，image_url 内联 base64，实测识别出日历截图：三栏布局/4 分类色/每格 3 条/无任何工时元素）。

### 方案要点
- **【V2 按用户反馈调整 2026-08-18】用户否掉周视图切换，要求保持月视图原貌；保留「复制本周清单」交互（点击弹出预览对话框可复制）→ 最终方案：月视图完全不动，仅底部加一条本周概览条（条数+分类小计+工时合计）+「📋 复制本周清单」按钮 → 点击弹出 Dialog 预览 Markdown 清单 + 复制到剪贴板。**
- 周视图 7 列方案已废弃（不再实施）。
- 工时规则：非 allDay 按时段差算，0.5h 向下取精度，end==start 不计入合计；allDay 不计工时；纯前端计算不落库。
- **可选增强（拍板项）**：① per-条目手工覆盖工时 hours 字段（v2 migration ALTER TABLE，对话框加工时输入）② 周报导出文件。推荐做①，成本约半天。
- 【约束】不改 schema（除增强①）；周一为周首；工时 0.5h 精度向下；allDay 不计；分类筛选同步生效；不新增 max-lines disable；i18n 五语言；复用现有 UI 原语不造新色。

### 产物（docs/** 被 gitignore 不入提交，本机可预览）
- 设计文档：`docs/2026-08-18-日历周视图与工时汇总设计.md`（V2：月视图+本周概览+复制清单；含 ASCII 线框+约束清单+影响面清单）
- 页面效果原型：`docs/2026-08-18-本周概览与复制清单原型.html`（自包含单文件，浏览器打开预览；月视图原貌+底部本周概览条+复制弹窗，以 2026-08-17~23 一周示例数据合计 16h/13 条；旧周视图原型文件已删除）

### 待办
- 评审通过后按设计文档第六节实施（calendar-time 纯函数+单测 → 新增 2 组件（概览条/清单对话框）→ CalendarPage 底部挂载 → i18n → 三 typecheck+单测+oxlint → 打包冒烟）。

## 实施：上游同步 v1.4.96→v1.4.99（142 commits）并入 custom（2026-08-18 下午）

用户要求：上游（stablyai/orca）更新了，同步最新版本。按既有流程：fetch upstream(代理) → main ff → custom merge main → 解决冲突 → pnpm install → 三套 typecheck → push → 打包。

**上游跨度**：`1f483c3e4 → 7a695c70f`（142 commits，2165 文件 +193795/-57662），tag 到 v1.4.99；上游在窗口内拆分了 persistence.ts（8b04e060f → `src/main/persistence/` 目录，主文件剩 8 行 barrel）、重做 artifacts 页、新增 skills 视图、换 xterm patch。

**遗留记忆先补 chore 提交**（4868cf28e：PR#7+fpr 记录），保证切分支时工作树干净。

**冲突 6 文件处置**（全保留 fork 逻辑）：
1. `config/electron-builder.config.cjs` — 保留 `productName: 'orca-s'`，**丢弃上游新增 `protocols: orca://` 注册**（避免 orca-s 与官方 Orca 抢深链，共存原则）。
2. `src/main/persistence.ts` — 上游拆目录后全取 main 的 8 行 barrel，再把定制 81 行按新结构重放：`loading-store/store.ts`（join/calendar 导入；Store 字段 customDb+calendarTable；构造器 `new CustomDb(join(dirname(dataFile),'orca-custom.db'),[CALENDAR_ENTRIES_TABLE_MIGRATION])`；构造器末 migrateLegacyCalendarEntries()；5 个日历方法插在 deleteAutomation↔createAutomationRun 之间；Gitee visibleTaskProvidersDefaultedForGitee 迁移块+loadNeedsSave+设置落点改 taskProviderSettingsWithGitee）+ `applying-settings/settings-update.ts`（gitee 标记）。【坑】相对路径随目录变化：`../../custom-db/...`、`../../../shared/...`。
3. `AppWorkspaceShell.tsx` — 标题条排除列表并集 automations/artifacts/calendar。
4. `SidebarNav.tsx` — lucide import 并集 BookOpen+Calendar（均在使用）。
5. `en.json` — 两冲突区用 **python 递归深度合并（HEAD 优先）**：`git show HEAD/main` → json.load → 递归 union → json.dump 写回 → json.tool 验证。【坑】json.dump 全文件重排缩进，diff 偏大可接受。
6. `ui.ts` — **不变式：previousViewBeforeX = TopLevelView 全集 − {X}**（全集=terminal/settings/tasks/activity/automations/calendar/space/skills/artifacts/mobile）；上游新增 previousViewBeforeSkills 后补 `'calendar'`、calendar 补 `'skills'`。

**上游自身缺陷**：`TabBarCreateEntry.tab-results.test.tsx` mock 缺上游 #15134 新增的必填 `occupantAgent`（tsc 报错，上游 CI 未在本地同配置捕获）→ 补 `occupantAgent: null` 修复，cherry-pick 到 custom（ab2f538b1）。

**并发会话注意**：日历周概览/工时汇总实现在 `calendar-week-summary` 分支进行中（calendar-time.ts 14:51 起持续写入），**我的修复提交误落该分支（ce5e50350），已用临时 worktree 把同一修复 cherry-pick 到 custom（ab2f538b1）**；custom 分支本身不受影响。打包 orca-s 待该分支合回 custom、工作树干净后再执行，避免把半成品打进 DMG。

**验证**：pnpm install --frozen-lockfile 通过（+9/-8 包，native rebuild 正常）；typecheck:node / cli 通过、typecheck:web 通过（含并发分支当前改动）。

**推送**：main `1f483c3e4..7a695c70f`、custom `fd6045238..ab2f538b1`，均走 127.0.0.1:54687 代理。

**约束清单核对**：GitHub fetch/push 走代理 ✅；main 纯 ff ✅；custom 合 main 后重打包（待做，见上）⚠️；大 JSON json.tool 验证 ✅；gitee 字段类型仍在 ✅；未直推、feature 分支工作流保持 ✅。

**归档**：影响说明 `docs/2026-08-18-上游同步v1_4_99影响说明.md`；本日记忆本文件。

## 答疑：GitHub 页面「152 files changed」是什么（2026-08-18 晚，Q&A，未动代码）

用户问「git 还有好多代码没提交，按功能类型创建 PR 提交」，并在 GitHub 页面看到「custom 分支 152 处已提交更改」。

**结论**：**152 = GitHub 对比页 files changed，不是未提交的代码**——这是 origin/custom 相对 origin/main 的全部累积差异（49 commits / 152 files / +9303 −234），等于 8 个已合并 PR（Gitee #1-3、日历 #4/6/8、zcode #5、tasks-nav #7）+ 上游同步 v1.4.99 + 打包配置直推的**总和**。

**已核实无需操作**：
- 工作区仅剩 .workbuddy 记忆文件（已提交）；全部分支与 origin 0 behind（`git branch -vv` 逐分支核对）。
- 唯一本地未推送：calendar-feature 分支 ahead 2（01784cc3c PR#4 记录 + 4a5412023 merge 提交，均为合并记录非新代码）；custom 本地在 ab2f538b1、origin/custom 已到 b88fc84a6（PR #8 合并）→ 已 ff 同步。
- 遗留 stash@{0}（gitee L2-B WIP，8/16）内容与 custom 现状一致（迁移已吸收），无价值可不恢复。
- 二开功能均已有对应 PR 且全部 MERGED：**无需再按功能创建 PR**。

**教训（重踩！T 类）**：本次在 custom 分支上误执行 `git checkout -- .workbuddy/memory/...` 丢弃了本会话记忆追加（14:5x-15:1x 的 V2 设计与实施完成段落）→ 靠 drop 前 stash 对象 354c1086f（rebase 前 stash "memory files"）恢复 96 行版本；MEMORY.md 恢复 13 行版本。**经验：切换分支前先处理/提交工作区记忆改动，绝不 `git checkout --` 覆盖 .workbuddy 下文件；记忆丢失先查 `git fsck --unreachable` + `git cat-file -p <hash>^1:.workbuddy/...` 从 stash/悬空对象恢复。**

## 实施完成：本周概览 + 复制本周清单（2026-08-18 下午，feature/calendar-week-summary → PR #8，已合并）

- 用户确认 V2 方案后实施，改动 8 文件 +643/-41（commit 753547575 已推 origin，**PR #8 已由用户合并（b88fc84a6）**）。
- 新增文件：`calendar-week-summary.tsx`（底部概览条：分类计数+共N条+合计X小时+复制按钮）、`calendar-week-list-dialog.tsx`（Dialog 预览 Markdown + 复制到剪贴板 + toast 成败提示）、`calendar-week-list.ts`（纯函数：groupWeekEntriesByDay/buildWeekListMarkdown）、`calendar-week-list.test.ts`（11 用例）。
- `calendar-time.ts` 新增：`weekRangeDates`（周一锚定含跨月）、`formatWeekRangeTitle`、`hourSpan`（0.5h 向下取整）、`summarizeWeekHours`、`collectWeekEntries`（lunarRepeat 按 viewYear 映射）。
- `CalendarPage.tsx`：三栏卡片外套 flex-col，底部挂概览条；新增 weekListOpen state；weekDates = weekRangeDates(selectedDateKey)（跟随选中日期所在周）。
- i18n：en/zh 各 +13 键（weekSummary/copyWeekList/weekList*/weekEntryCount/weekTotalHours）；es/ja/ko 回退英文（历史惯例）。en/zh.json 用 python json.dump 重写后补回末尾换行（printf '\n' >>），json.load 验证通过。
- 验证全绿：三 typecheck、日历相关单测 75/75（原 64+新 11）、oxlint 0 警告 0 错误、electron-vite build 成功、localization-extraction/catalog 过，i18n 键已确认进打包产物。
- 【坑-oxlint】`unicorn(prefer-at)`：负数数组索引 `arr[arr.length-1]` 报 error → 改 `.at(-1) ?? arr[0]`；useMemo 依赖含每次重建的 strings 对象报 exhaustive-deps warning → 清单生成够轻量，直接去掉 useMemo 用普通常量。
- 【坑-并发分支】另一会话（上游同步 v1.4.99）的 mock 修复提交误落我分支（ce5e50350），且 custom 已有等价 cherry-pick（ab2f538b1）→ rebase --onto origin/custom 去掉无关提交再 force push，PR 变单提交；**教训：多会话并行时提交前 git status 检查分支归属**（历史 Gitee L2-B 同类事故再犯，已成重踩）。
- 约束自查：不改 schema ✓、周一为周首 ✓、0.5h 向下取整 ✓、allDay/end==start 不计工时 ✓、月视图布局原样 ✓、分类筛选同步生效 ✓、无 max-lines disable ✓、i18n 惯例 ✓、复用 Dialog/Button 原语 ✓。
- 打包冒烟未做（改动纯 renderer，build 已过；待 PR 合并后如需装机再 build-orca-s）。