# 2026-09-03 上游同步 v1.4.186→9cda5a9dc0 与版本号对齐影响说明

> 背景：用户要求拉取远端重打，且包版本不要停留在 1.4.178（更新弹窗显示官方已到 1.4.196）。
> 结论：main 快进 88 commits 到 `9cda5a9dc0` 并合入 custom（`88c5c304aa`），
> 打包基线改为 HEAD 可达最新正式 tag（现 v1.4.196）+ local 后缀；
> 新包 `1.4.196-local.1788401316055.88c5c304aaaa` 已安装启动。

## 一、版本问题结论（先回答用户原问）

- 查实：custom 与 upstream/main 的 `package.json` 都是 `1.4.178-rc.2`；
  弹窗的 1.4.196 来自官方 release feed（release-cut 在切版时打 tag，
  主线 package.json 天然滞后）。v1.4.196（09-03 01:04 UTC）早于本次同步到的
  主线 HEAD（09-03 01:13 UTC）约 9 分钟，主线已包含该切版全部内容。
- 做法（用户已确认"保持后缀"）：`build-mac-local.mjs` 新增
  `resolveVersionBase()`，取 `max(package.json, 最新正式 tag)` 做基线，
  后缀规则不变 → `1.4.196-local.<ts>.<commit>`。
- 提醒：更新弹窗以后还会出现（查的是官方 feed），一律忽略，不要点，
  否则可能被官方包覆盖。

## 二、合并冲突（3 处，全保留 fork 逻辑）

1. `config/electron-builder.config.cjs` — 取上游 `toolsets` pin，
   丢 `orca:// protocols`（沿用共存原则），`productName: 'orca-s'` 不变。
2. `src/main/startup/main-process-quit.ts` — 两边都要：
   fork 的 `stopBrowserAutomationMcpServer()` + 上游 `cancelHistoryGc()`。
3. `src/renderer/src/components/sidebar/SidebarNav.tsx`
   - 日历 hooks/flags：保留（store 动作仍在）。
   - **上游删掉了整个 experimentalActivity 侧栏区**（含 fork 的 Agents
     入口+未读 badge），合并后 fork 出现回归 → 已按 HEAD 原样恢复，
     仅适配上游改无参的 `useActivityUnreadCount()`。
   - import 并入上游单行风格。

## 三、合并触发的全量 lint 修复

- 背景：平时提交只 lint 暂存文件；合并暂存 1000+ 文件触发全量门禁，
  暴露两处历史欠账（均在 fork 自有代码）。
- `SidebarNav`：`Bell/openActivityPage/activityActive` 在恢复 Agents 入口后
  重新被使用（恢复前曾短暂 unused）。
- `CalendarPage.tsx`（fork 自有，上游无此文件）：430 行超 400 上限 →
  删除确认框抽为 `calendar-entry-delete-dialog.tsx`（407 行，门禁过）。

## 四、验证

- `verify-features.mjs` 全绿；pre-commit hooks 全过；新增单测
  `build-mac-local.test.mjs`（resolveVersionBase）+ 日历单测共过。
- 构建一次成功（含 typecheck），`codesign --deep --strict` 通过，
  dist/install 双验签；dist 残留旧 178 zip 未删（无害）。
- 新包主进程存活 + renderer 有实时 trace 写入；孤儿 helper（8-30/9-2
  及今早 SIGTERM 遗留）已清理 50+ 个。
- 待用户目检：窗口可见 + 关于页版本 `1.4.196-local…`。

## 五、遗留

- 未 push（等用户确认再推 custom 到 origin）。
- 日历外挂迁移仍待用户在 UI 填路径 + 重启（见当日日记忆）。
