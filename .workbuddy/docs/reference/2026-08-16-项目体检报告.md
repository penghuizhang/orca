# Orca 项目体检报告（stablyai/orca）

- 日期：2026-08-16
- 上游仓库：https://github.com/stablyai/orca
- 本地 fork：/Users/zhangpenghui/code/aistudy/2026/fork/orca（penghuizhang/orca，main 与上游同步，0 ahead / 0 behind）
- 数据口径：本地 git + 文件系统实测（2026-08-16），GitHub API 一次拉取；repomix-output.xml 不存在，直接读仓库

## 1. 项目类型

**Agent 开发环境（ADE，Agent Development Environment）桌面应用** —— 不是 Chat UI / RAG / IDE 插件，本身就是完整 IDE。

| 依据 | 路径 / 行号 |
|---|---|
| `"description": "Next-gen IDE for parallel agentic development"` | package.json:4 |
| "Run Codex, ClaudeCode, OpenCode or Pi side-by-side — each in its own worktree" | README.md:19-21 |
| 结构：Electron 主进程 + React 渲染层 + CLI + Node daemon + RN 移动端 + 原生模块 | src/main、src/renderer、src/cli、mobile/、native/ |
| 内置完整编辑器能力（Monaco、xterm、嵌入式浏览器、git worktree 编排） | package.json devDependencies monaco-editor/xterm（:202,:223）；README.md:49-157 |

## 2. 技术栈与占比

全仓 15,331 个文本文件、约 **322.5 万行**（排除 node_modules/.git/out/dist/二进制，实测 `find+wc`）：

| 语言 / 类型 | 文件数 | 行数 | 占比 |
|---|---|---|---|
| TypeScript (.ts) | 12,680 | 2,490,143 | 77.2% |
| TSX (.tsx, React) | 1,818 | 460,936 | 14.3% |
| JSON | 38 | 96,866 | 3.0% |
| mjs / cjs / js（构建脚本等） | 376 | 74,027 | 2.3% |
| YAML | 3 | 15,392 | 0.5% |
| CSS | 7 | 9,025 | 0.28% |
| Swift（macOS 原生） | 30 | 7,096 | 0.22% |
| HTML / MD / Python / Shell | 41 | ~9,000 | 0.3% |

关键结论：

- **前后端同构，全部 TypeScript**：前端 UI = `src/renderer/`（React 19 + shadcn/ui + Tailwind 4 + Zustand + Monaco + xterm.js）130.7 万行；后端 = `src/main/`（Electron 主进程 + Node daemon，含 ipc/ssh/runtime/orchestration）107.9 万行；另有 `src/shared/` 16.7 万行、`src/relay/` 6.0 万行、`src/cli/` 3.6 万行。前后端占比约五五开。
- **原生模块多语言**：Swift（computer-use-macos、notification-status-macos）、Python（computer-use-linux）、PowerShell（computer-use-windows）、C#（windows-cli-launcher）。
- **移动端** `mobile/` = Expo/React Native，独立 pnpm workspace（约 19 万行）。
- **测试占比惊人**：`.ts/.tsx` 中 52%（153.7 万行）是 `*.test.ts` 文件；最大单文件 `orca-runtime.test.ts` 5 万行、`orca-runtime.ts` 3.9 万行。

## 3. 入口文件

| 入口 | 命令 / 配置 | 文件路径 |
|---|---|---|
| 本地开发 | `pnpm dev` | package.json:52 → config/scripts/run-electron-vite-dev.mjs |
| 构建后运行 | `pnpm start`（electron-vite preview） | package.json:51 |
| Electron 主进程 | electron.vite.config.ts:208-209 input.index | src/main/index.ts |
| Node daemon | electron.vite.config.ts:212 | src/main/daemon/daemon-entry.ts |
| plugin host / computer sidecar | electron.vite.config.ts:213-214 | src/main/plugins/plugin-host-entry.ts、src/main/computer/sidecar-entry.ts |
| CLI | bin `orca` → ./out/cli/index.js | package.json:7-8；源码 src/cli/index.ts |
| 官方本地流程 | `pnpm install` + `pnpm dev` | .github/CONTRIBUTING.md:33-36 |

无 Makefile / docker-compose（config/docker 仅为 SSH 场景辅助镜像）。

## 4. 核心模块边界

| 类别 | 目录 | 行数（ts/tsx/js/mjs） |
|---|---|---|
| **核心** | src/renderer（前端 UI） | 1,307,079 |
| **核心** | src/main（主进程 + daemon 后端） | 1,078,782 |
| **核心** | src/shared（前后端共享） | 166,703 |
| **核心** | src/relay（远程中转） | 60,414 |
| **核心** | src/cli（命令行） | 35,775 |
| **核心** | mobile/（RN 移动端，独立 workspace） | ~190,000 |
| **核心** | native/（Swift/Python/PS/C# 原生模块） | ~8,500 |
| **核心（扩展面）** | skills/（8 个内置技能，markdown 定义） | 8 文件 |
| 配置/脚手架 | config/（构建、质量门禁、patches、353 个 .mjs 脚本） | 58,390 |
| 测试工具 | tests/（playwright e2e、bench 工具） | 113,413 |
| 文档/资产 | docs/、resources/、examples/、.github/、Casks/、skill-guides/、skill-stubs/ | — |
| 二开本地 | .workbuddy/（记忆 + 文档，非上游内容） | — |

## 5. 依赖复杂度

| 范围 | 运行时 | 开发 | 可选 | 合计 |
|---|---|---|---|---|
| 根 workspace | 23 | 100 | 6 | 129 |
| mobile（独立 workspace） | 46 | 15 | 0 | 61 |

特殊版本 / 环境要求：

- **Node 24 硬性要求**（package.json:279-281 `"engines": {"node": "24"}`）
- **pnpm@10.24.0 精确锁定**（package.json:282 `packageManager`，含 sha512）
- 5 个 `patchedDependencies`：node-pty 与 xterm 系列（package.json:307-313）；pnpm overrides（:284-286）
- 原生模块需 node-gyp 编译：node-pty、sherpa-onnx、@parcel/watcher（onlyBuiltDependencies :300-306；postinstall → config/scripts/rebuild-native-deps.mjs :77）
- git-lfs（.gitattributes）
- 无 Python 版本要求（native/computer-use-linux 仅 2 个脚本）；无 Docker Compose 开发依赖

## 6. License

**MIT**（LICENSE:1-22，Copyright (c) 2026 Lovecast Inc.，LICENSE:3）。商用允许：使用/复制/修改/合并/发布/分发/再许可/销售全部放开（LICENSE:5-10），无传染性条款、无额外限制（GitHub API 确认 license=MIT）。注意：版权方是 Lovecast Inc. 而非 README 中露出的 stablyai 品牌。

## 7. README 质量与社区活跃度

README（16.6 KB）质量高：徽章、hero、9 大功能图文表、20+ 受支持 agent 清单、多平台安装（官网下载/brew/AUR/移动端）、社区渠道（Discord/X/微信群）、6 种语言版本（README.md:15）。**快速启动不在 README 内**，指向 CONTRIBUTING.md（README.md:253），其中 Local Setup = `pnpm install && pnpm dev`（CONTRIBUTING.md:33-36）。

社区活跃度（GitHub API 2026-08-16 实测 + 本地 git）：

- 46,181 stars / 3,234 forks；2026-03-17 建仓（仅 5 个月）；3,935 open issues
- 本地 git：8,747 commits，最近 30 天 1,997 个（≈66 个/天）；PR 编号已过 #14824
- 上游 latest release v1.4.183 发布于 2026-08-15（前一天），几乎每日发版
- 主要贡献者：Neil 2,938、Jinjing 1,678、Brennan Benson 1,292、Jinwoo Hong 938、github-actions 713 commits
- **issue 响应速度无法从 repo 内推断**（需要 GitHub issue 侧时间数据）；日更节奏与高合并量是活跃度侧证

## 8. 二开难度初判：4 / 5（偏高）

| 因素 | 方向 | 依据 |
|---|---|---|
| 代码体量 | - | 322 万行 / 15,331 文件 / 8,747 commits；核心文件单文件 3.9 万行（src/main/runtime/orca-runtime.ts:1） |
| 技术栈广度 | - | Electron + Node daemon + React + RN + 4 种原生语言 |
| 质量门禁 | - | lint 链含 oxlint + max-lines-ratchet + reliability-gates + 多语言 i18n 校验（package.json:14）；AGENTS.md 禁止 max-lines disable |
| 上游同步压力 | - | 66 commits/天、每日发版，fork 需频繁 merge |
| 测试负担 | - | 52% 行数是测试，改核心要过大量单测 + playwright e2e（package.json:86-111） |
| 许可 | + | MIT，无法律障碍 |
| 扩展机制 | + | skills/（markdown 技能）+ orca.yaml + CLI（package.json:7-8）允许部分定制不动核心 src/ |

结论：整体 **4 星（难）**；若定制面收敛在 skills/配置/CLI 层，实际难度可降到 2-3 星。

## TL;DR（≤200 字）

Orca 是 MIT 协议的 Agent 开发环境 Electron 应用：建仓仅 5 个月即 46k stars、日更 66 commits、每日发版，社区极活跃。前后端全 TypeScript，322 万行代码中 52% 是测试；Node 24 + pnpm 10.24 强锁定，带 RN 移动端与 Swift/Python/C# 原生模块，质量门禁极严。二开整体难度 4/5；但 MIT 许可 + skills/ 扩展机制 + CLI 让「新增型定制」可绕开核心源码，值得投入。

## 附：数据口径与备注

- 行数统计命令：`find <dirs> -type f -name "*.<ext>" ! -path "*/node_modules/*" -exec cat {} + | wc -l`；目录为 src/mobile/native/config/skills/tests
- 测试行数口径：全仓 `*.test.ts / *.test.tsx / *.spec.ts`
- 本报告为纯调研，未修改任何代码、未安装运行、未做架构图（留待环节 3）
