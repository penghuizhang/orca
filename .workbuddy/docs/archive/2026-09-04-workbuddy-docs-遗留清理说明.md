# .workbuddy/docs/ 遗留文档清理说明

日期：2026-09-04
状态：✅ 确认完成，待用户决定是否迁移

## 现状

`.workbuddy/docs/` 下只有 1 篇文档：
- `2026-08-16-本地开发环境搭建.md` — 早期本地环境搭建记录

仓库根 `docs/` 下有 40+ 篇设计文档，均为后续产出。

## 根因

按 AGENTS.md 规范，设计文档放仓库根 `docs/`，日记忆放 `.workbuddy/memory/`。早期那篇是二开第一天写的，当时 `docs/` 尚未建立，后来所有文档都统一放到了仓库根 `docs/`。

## 建议

1. **迁移**：将 `.workbuddy/docs/2026-08-16-本地开发环境搭建.md` 移到 `docs/` 下统一管理
2. **或删除**：该文档内容偏个人环境记录，团队共享价值低，可直接删除

用户拍板后执行。
