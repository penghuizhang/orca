# 上游同步 v1.4.99 影响说明（2026-08-18）

## 概要

按既有同步约定：`main` 快进到 `upstream/main`（7a695c70f，v1.4.99），再并入 `custom`。
上游跨度 142 commits、2165 文件（+193795/-57662），是近期最大一次同步（含一次大规模重构）。

## 冲突与处置（6 文件，fork 业务逻辑全保留）

| 文件 | 冲突内容 | 处置 |
|------|----------|------|
| `config/electron-builder.config.cjs` | 上游新增 `protocols: orca://` 注册 | **丢弃**——orca-s 不注册 orca:// scheme，避免与官方 Orca 抢深链（共存原则） |
| `src/main/persistence.ts` | 上游拆目录重构（8b04e060f），main 侧为 8 行 barrel | 全取 main 结构，按新目录重放定制：`loading-store/store.ts`（日历 CustomDb/CalendarTable 字段+构造器+5 方法+migrateLegacyCalendarEntries；Gitee visibleTaskProvidersDefaultedForGitee 迁移）、`applying-settings/settings-update.ts`（gitee 标记） |
| `src/renderer/src/app-shell/AppWorkspaceShell.tsx` | 标题条排除列表 | 并集：automations/artifacts/calendar |
| `src/renderer/src/components/sidebar/SidebarNav.tsx` | lucide import | 并集：BookOpen+Calendar（均在使用） |
| `src/renderer/src/i18n/locales/en.json` | 两处（nav 标签；artifacts 新子部件键 vs calendar/GiteeItemDialog 兄弟键） | python 递归深度合并（HEAD 优先），json.tool 验证 |
| `src/renderer/src/store/slices/ui.ts` | 上游新增 previousViewBeforeSkills、扩展 previousViewBeforeSpace | 按不变式「previousViewBeforeX = TopLevelView 全集 − {X}」补齐：skills 加 calendar、calendar 加 skills |

## 上游结构变化（对本 fork 有触点）

- **persistence 模块化**：`src/main/persistence/` 目录（loading-store/applying-settings/restoring-sessions/scheduling-automations/tracking-repos/leasing-ssh-ptys），主文件剩 barrel。此后同步 persistence 相关改动冲突面会变小，但**我们的定制代码需跟随新文件位置**（store.ts / settings-update.ts）。
- 新增 artifacts 页全宽表格 + drawer（上游）、skills 视图进入 TopLevelView。
- xterm 6.1.0-beta.287 patch 更新（≈7.4MB），`pnpm-lock.yaml` 微调（+9/-8 包）。

## 验证

- `pnpm install --frozen-lockfile`：通过（10.8s，native rebuild 正常）。
- typecheck:node / typecheck:web：见执行结果（合并提交 00a83662d 后运行）。
- 日历数据路径不变：orca-custom.db（<userData>/orca-custom.db）继续由 Store 构造器初始化，calendar_entries v1 迁移幂等。
- Gitee 字段 `visibleTaskProvidersDefaultedForGitee` 在 shared 类型中确认存在，迁移逻辑在新 store.ts 重放。

## 待办

- push main + custom 到 origin。
- 重新打包 orca-s（`node config/scripts/build-orca-s.mjs`，只能 custom 分支）。

## 归档

- 设计/影响文档：本文件
- 日记忆：`.workbuddy/memory/2026-08-18.md`（同步段落后追加）